Dual-Maintenance Workflow:
Maintaining two versions of the Memory Bank leverages the strengths of the AI and its token management system
Version 1. Lean/Concise memory-bank/activeContext.md
This file is loaded every session. It contains only the mandates and current context needed for immediate coding. Ensures quick, cheap context loading.V
Version 2. Verbose/Detailed version as a complete reference (`memory-bank/activeContext_history.md`)
Reference Only: This file is only read when needed (e.g., debugging, checking past architectural decisions)
To make this work, you must update the CLAUDE.md (or your main instructions) and then give CC a clear, multi-step task.
Step 1: Update the Core Mandate (CRITICAL) Modify the "Memory Bank System (CRITICAL)" section in your main CLAUDE.md to reflect the new file structure and mandate.

***** The Prompt to Claude Code at the end of a session:

"I have concluded this coding session.

**Please review our entire chat history since the last memory bank update and perform the memory bank update.** This is a multi-step task, so use the Agent."

**Task Details for the Agent:**

    1. **memory-bank/memory-bank-activeContext.md** (CONCISE - MAX EFFICIENCY):
     - Generate ONE summary sentence per completed task
     - Add ONLY to existing tables using | syntax
     - NO new paragraphs, NO explanations, NO prose
     - Example: | **WOD Search** | Multi-word OR search, movement filters, 2-column layout. | `app/coach/page.tsx:72-234` |
     - Keep total file under 10,000 characters
     - Update Version/Timestamp only

  2. **project-history/session_history.md** (VERBOSE):
     - Full technical details go HERE
     - All explanations, code snippets, debugging steps
     - This is the "dump everything" file
     - Located OUTSIDE memory-bank/ to avoid Cline reading it unnecessarily

  3. Use general-purpose agent.

  🎯  Your Cline Workflow (When Claude API Hits Limit): 🎯

  Switch to Cline Development:

  git checkout cline-dev

  In VS Code:

  1. Open Cline panel
  2. Click "Model" dropdown
  3. Select: x-ai/grok-code-fast-1 (FREE) or cline/code-supernova-1-million (FREE)
  4. Tell Cline: "You can READ memory-bank/ files for context, but DO NOT MODIFY or UPDATE them. Only Claude (Sonnet) updates the memory bank."
  5. Work on features/fixes

  When Claude Access Returns:

  # Review what Cline did
  git checkout main
  git log cline-dev --oneline -10  # See Cline's commits

  # If changes are good:
  git merge cline-dev
  git push

  # Tell me: "Review cline-dev commits and update memory bank"

  Current Status:

  - 🟢 main branch (current) - Your safe working version
  - 🔵 cline-dev branch - Ready for Cline experiments
  - 📦 Two backups on Desktop
  - ✅ All changes committed and pushed

  You're all set! When you hit the API limit, just switch to cline-dev branch and fire up Cline with Grok.


  2. Grok Task Files Created 31.10
   ✅

  grok-tasks-simple.md - 3 Simple UI Tasks:
  - Task 1: "Book a Class" button on Athlete page
  - Task 2: "Late Cancellation" button in Session Modal
  - Task 3: Visual distinction for attended workouts (teal background)

  grok-tasks-medium.md - 2 Medium Tasks:
  - Task 4: Resizable/draggable Session Modal
  - Task 5: Week navigation for Logbook & Published Workouts

  1. grok-tasks-simple.md - 3 Simple UI Tasks

  - Task 1: Add "Book a Class" button to Athlete page
  - Task 2: Add "Late Cancellation" button to Session Modal
  - Task 3: Visual distinction for attended workouts (teal background)

  2. grok-tasks-medium.md - 2 Medium Complexity Tasks

  - Task 4: Make Session Modal resizable and draggable
  - Task 5: Add week navigation to Logbook and Published Workouts

  All files include:
  - Exact file paths
  - Line-by-line implementation instructions
  - Code snippets to copy/paste
  - Testing steps
  - Git workflow reminder



  > NOPE! There is a published Workout on 30.10.25 (yesterday). On the card in the Weekly calendar view it shows 7:00. If
click the "2/12" badge, the session modal opens. The time there is 8.45. If I click to edit it, it defaults to 00:00. As I
will never need to program anything at 00:00 it would be ideal if it defaulted in the middle to 12:00 (it doesn't do that
now, it did before). If I edit the time in this modal, I get a "Time updated successfully" message (this is unnecessary) and
 it updates on the card. If, however, I click on the same card and go into the "Edit Workout" modal and change the time
there, it doesn't change on the card when I save and exit, but if I then enter the Session Management Modal, the time
displays correctly.
