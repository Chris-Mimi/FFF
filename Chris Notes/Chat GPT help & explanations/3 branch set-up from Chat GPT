Claude, please set up a three-branch fail-safe workflow for the web app project. Follow these rules exactly:

1. **Branches**:
   - `main` → stable, production-ready branch (last known working commit). Do not modify directly.
   - `augment-refactor` → branch where Augment Code will perform structural refactor of large TSX files (component splitting, linting, unused hook removal, import cleanup).
   - `claude-review` → branch where you will review and validate logic, state, and hooks after Augment finishes. Only semantic/logic edits allowed here.

2. **Workflow**:
   a. Ensure `main` is up-to-date and verified to be fully functional.
   b. Create or switch to `augment-refactor`.
   c. Run Augment Code on the large TSX file(s) to split components and clean the structure. Commit changes with message: "[AUGMENT] Structural refactor <filename>".
   d. Once Augment completes, create or switch to `claude-review` from `augment-refactor`.
   e. In `claude-review`, review **each component individually**:
      - Verify props, state, and context flows.
      - Ensure hooks are used correctly.
      - Fix semantic or logical errors only. Do not touch structural edits or formatting done by Augment.
   f. Run local testing / dev server to confirm each component works and renders correctly.
   g. Document results in a session log table.
   h. Merge `claude-review` back into `main` only after you and the human have fully verified functionality.

3. **Fail-safe rules**:
   - If any logic or rendering breaks, do not push to `main`.
   - Keep all original `main` commits untouched.
   - Always commit checkpoints before switching branches or agents.

4. **Optional verification**:
   - Use Cline/Grok to render components and check UI or schema without using tokens.
   - Use GitHub Copilot only for small utility or boilerplate code when needed; do not conflict with Augment or Claude.

Generate a step-by-step plan for me to execute this workflow automatically, including git commands for creating/switching branches and committing checkpoints. Provide the output as a concise instruction set suitable to follow in VS Code.


# one shot prompt for Claude from Chat GPT #
Claude, set up the three-branch workflow for the web app project:

1. Confirm `main` is up-to-date.
2. Create `augment-refactor` branch from `main` (or switch if it already exists).
3. Create `claude-review` branch from `augment-refactor` (or switch if it already exists).
4. Verify the current branch after each switch.
5. Commit any existing changes in each branch with:
   - `[AUGMENT] Structural refactor <filename>` for Augment edits
   - `[CLAUDE] Semantic review <filename>` for your review edits
6. Ensure `main` is never modified until final merge.
7. Log session in `session_log.md`.
8. Output a step-by-step execution plan including git commands to create, switch, and commit in all three branches.
