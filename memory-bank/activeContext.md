# Active Context

**Version:** 276
**Updated:** 2026-08-26 (Session 408 — Opus 5. **Movement Info bar — exercise descriptions in the workout builder. 1 commit (`a9399bb`), pushed, tsc+lint+build clean; Chris will test live.** The create/edit modal's top bar auto-detected exercises in the section text but only surfaced the **298 of 716** with a `video_url`; **713 have a written `description`** that was reachable only as a native `title=` tooltip in the Movement Library popup — unreadable for multi-level drills like the Ring Muscle-Up Floor Assisted (529 chars). Fixed in 4 files: [section-video-matcher.ts](utils/section-video-matcher.ts) now keeps an exercise with a video **OR** a description and carries the text through (`MatchedExerciseVideo`→`MatchedExerciseInfo`; **name-matching logic untouched**, incl. the S384 longest-match pass); new [MovementInfoModal.tsx](components/coach/MovementInfoModal.tsx) shows it `whitespace-pre-wrap` at `z-[105]`, deliberately **below** the video modal's `z-[110]` so Play opens on top; [MovementDemosBar.tsx](components/coach/MovementDemosBar.tsx) chips are now two buttons — **▶ plays the video (still ONE click, no regression), the name opens the description** (blue ℹ) — label renamed *Movement Demos*→*Movement Info*; [useWorkoutModal.ts](hooks/coach/useWorkoutModal.ts) selects `description` and **paginates** the fetch. **Rejected a separate description bar** (option 6): the video set is almost entirely a subset of the description set, so movements would appear in both rows, and two headers eat space above the sections. **Intent/Stimulus field left at `maxLength={500}`** — Chris decided this feature covers the need. ⚠️ **Main pitfall: `exercises` is at 716 of the silent 1000-row PostgREST cap and only `useWorkoutModal` is paginated** — see Known Open Issues.)

<!-- Older S407: 2026-08-25 (Session 407 — Opus 4.8. **Members/Planner/Subscriptions batch — 8 commits + 2 migrations, all shipped, pushed, tsc+build clean, tested live by Chris.** **(1) Subscriptions Due banner overhaul (`5dcd440`):** lapsed warnings now **persist until the coach renews or dismisses them** (removed the 14-day auto-drop on both cash + Stripe lapsed queries); **cash grace 4→7 days** before the coach-side auto-expire cuts access ([useMemberData.ts](hooks/coach/useMemberData.ts)); athlete [PaymentDueBanner.tsx](components/athlete/PaymentDueBanner.tsx) `GRACE_DAYS` 4→7 so the in-app "pay" reminder covers the full week. Dismissed 2 ex-beta-testers (Torben Stoffer, David Montgomery) from the banner (set `lapsed_banner_dismissed_at`). **(2) Planner usage-recency dots in the Movement Library (`b266eb4`+`3bf5716`):** the workout create/edit Movement Library popup now shows a recency dot (same bands as the Planner chips: ≤2wk green … 3mo+ grey … never) on **all four tabs** (Exercises incl. Favorites/Recent, Lifts, Benchmarks, Forge), with a "last programmed" tooltip + shared legend. New [utils/exercise-recency.ts](utils/exercise-recency.ts) = single source of truth for the bands + a `makeRecencyMapLoader` cache factory (one session-cached full-history map per library type, keyed by that type's id). Planner `PatternExerciseChips` de-duped onto it. **(3) Wellpass — linked Lani Neumann to mum Katja's household (`3c9ffd9`, DB-only):** no UI exists for a 2nd household member (Excel sync only auto-links one member per Wellpass name), so inserted the `wellpass_identity_members` row directly; script kept. **(4) Park/Block reason feature (`2c7b29a`+`95bc2e0`+`2734a6a`, migrations `20260824000000` then `20260825000000`):** coach can record WHY on park/block — the shared confirm dialog gained an optional text field (`confirmWithReason` in [lib/confirm.ts](lib/confirm.ts)+[ConfirmDialog.tsx](components/ui/ConfirmDialog.tsx), keeps Block's red danger styling); inline "+ add reason"/edit on Parked & Blocked rows for the 23 pre-existing parked members ([MemberCard.tsx](components/coach/members/MemberCard.tsx) `ReasonEditor` + [status-reason route](app/api/members/status-reason/route.ts)). **Then hardened for privacy:** reasons were columns on `members`, which an athlete can read for their own row (RLS `auth.uid()=id`) — a parked athlete keeps full access → could fetch their own reason. Moved to a **coach-only `coach_member_notes` table** (RLS grants only `role=coach`; no athlete policy). Writes via service role (best-effort, never fail the action), coach list-read merges them in for Parked/Blocked tabs only, `members` columns dropped. **Verified with anon key: non-coach read returns 0 rows even though the row exists.** **(5) Parked-athlete rebook notification (`aec03be`):** a parked athlete still has booking access, so when one self-books, coaches now get a push + notification-log alert ("Parked athlete is back — Restart them?") linking to Members; service-role parked-flag read in [bookings/create](app/api/bookings/create/route.ts), fire-and-forget. See ⚡ Next Session Kickoff.) -->

<!-- Older S406: 2026-08-23 (Session 406 — Opus 4.8. **2 DB admin tasks + a NEW morning-session auto-lock feature + coach lock visibility. All shipped, pushed, tsc+build clean, tested live by Chris.** **(1) Planner 'Pre-Workout' group bulk-fill:** linked all 90 remaining Pre-Workout-category exercises to the existing 'Pre-Workout' movement pattern (59→149) via service-role [add-preworkout-group.ts](scripts/add-preworkout-group.ts) — INSERT-only, deduped. **(2) Koffler family 10-card:** moved Anton + Viktoria onto Irene's shared card ([link-koffler-kids-to-card.ts](scripts/link-koffler-kids-to-card.ts)) — each kid `ten_card_holder_id`→Irene + `primary_payment_method`→`ten_card`; cleared Viktoria's stale own card (0 used, nothing lost). Irene pays `member` so her own bookings don't burn the kids' card (the Miriam-Jacht pattern; effective method gate in create/cancel/useMemberData). **(3) 🔒 NEW FEATURE — morning-session auto-lock (`d7b99d1`):** sessions starting before `morning_cutoff_time` (default 12:00) stop taking bookings at `morning_lock_time` (default 20:00) on the PREVIOUS day, regardless of session type — solves morning/evening classes sharing a `workout_type` so the per-type lead-lock couldn't isolate mornings. New `booking_rules` cols `morning_lock_enabled/cutoff_time/lock_time` (migration `20260823000000`, **Chris ran it**), OFF by default (now enabled in prod). Shared TZ-safe helper `sessionAutoLockInstant` ([lib/bookingRules.ts](lib/bookingRules.ts)) = lead-time lock ∧ morning rule, earliest wins; used by ALL lock checks (create/cancel/book page) so they stay identical. Admin → Booking Rules toggle + two time pickers. **(4) Coach lock visibility (`55368a1`):** calendar cards show a gray 🔒 Locked badge for effectively-locked FUTURE sessions (manual OR auto; tooltip distinguishes), computed from public rules to match the athlete view ([useCoachData.ts](hooks/coach/useCoachData.ts) + [CalendarGrid.tsx](components/coach/CalendarGrid.tsx)); session-modal Lock indicator now uses the shared helper ([useSessionEditing.ts](hooks/coach/useSessionEditing.ts)) instead of a raw start-time check. Root cause of Chris's 'locked in athlete app but not coach app' = coach had NO lock indicator + modal ignored auto-lock. Verified live: D&D 09:00 locks the evening before, evening classes stay open, coach badge shows. See ⚡ Next Session Kickoff.) -->

<!-- (spacer) -->

<!-- Older S405: 2026-08-21 (Session 405 — Opus 4.8. **Mobile Planner UX (2 fixes) + intermittent attendee-list bug (2 real bugs found & fixed). 5 commits, all pushed, tsc clean. All coach/athlete-facing; awaiting Chris's live test.** **(1) Planner — hide "exercises" word on mobile pattern rows (`299b010`):** [PatternManager.tsx:318](components/coach/analysis/PatternManager.tsx#L318) count label wrapped so mobile shows just the number (e.g. `3`), `sm+` shows `3 exercises` — frees horizontal space so more of the pattern name shows (Chris: names truncating to 1-2 letters on mobile). Name column still has `truncate`, so column width is the remaining constraint if names still clip. **(2) Planner — Session sub-filter wrap on mobile (`f34ae2d` + `df5cf6d`):** the Adults All/WOD/Foundations chips ([PlannerSection.tsx:670](components/coach/analysis/PlannerSection.tsx#L670)) ran off the right edge — the inner Track+Session flex row had no `flex-wrap`. Added it; then grouped the "Session:" label + its chips into one `flex items-center gap-2` unit so they wrap together (first attempt left the grey label stranded alone above the chips). **(3) 🐛 Intermittent attendee list — TWO bugs (Michael Städele: "5/10 booked but no names underneath", persists even after others book).** The count and the names come from different sources — count is baked into the session list; names come from a separate `/api/bookings/attendees` GET. **Bug A — sole-attendee crash (`28ab186`):** [attendees/route.ts:68](app/api/bookings/attendees/route.ts#L68) looped `sessionBookings[sid]` without the `?? []` guard used later at line 93. A session where the viewer is the only confirmed non-OG/non-trial booking → `sessionBookings[sid]` undefined → `for...of` throws → **500 wipes names for the WHOLE batched request** (all his booked sessions). Fires for early bookers (Michael books first); self-heals as others book → intermittent. **Bug B — stale phone cache (`db2ad5e`):** the attendee fetch was a plain GET, no cache directive, URL stable per booked-session set → mobile browsers cached an early empty/partial response and kept serving it **even after members booked in**. Added `cache:'no-store'` on the client fetch + `Cache-Control:no-store` on the API response. Bug B is the one that matches "still empty after they booked". **Untouched 3rd suspect (weak):** if `getSession()` is null mid-refresh ([book/page.tsx:348](app/member/book/page.tsx#L348)) the fetch silently skips until next load — didn't touch working auth code on a hunch; if it recurs after this deploy, ask Chris whether a full reload fixes it (→ token path) or not (→ elsewhere). See ⚡ Next Session Kickoff.) -->

<!-- (spacer) -->

<!-- Older S404: 2026-08-15 (Session 404 — Opus 4.8. **Planner pattern-colour overhaul (6 commits, all pushed, tsc+build clean, no DB-data risk) + an accidental WOD-overwrite recovery investigation (no code — unrecoverable, Chris recreated manually).** All coach-facing, tested live. **(1) Bigger group dots** (`c236a33`): Movement Patterns colour swatch `w-3`→`w-5` (12→20px); stays within the row's existing height (action-icon buttons already set it) so rows don't grow. **(2) Click-to-pick colour popover** (`ed34e6f`): new shared `ColorDot` component — clicking a pattern's dot (and the new-pattern swatch) now opens a small palette popover to choose from directly instead of cycling one colour per click; closes on selection or outside-click. **(3) Palette iterations** (`8c82716`→`c7ca2f9`→`2b6c215`→`cfa4c34`): fixed the rose-pink that read like red, added colours, then rebuilt as a spectrum-ordered max-diversity 12, then **applied Chris's own 12-colour pick**. Final `PATTERN_COLORS` (maroon→pink): `#770909,#F20202,#F99639,#E8F86D,#80F982,#8AFFF3,#C2DFFF,#5196D6,#1A0BBC,#830BF4,#E367F4,#F4CDE0`. **Built an interactive colour-picker Artifact** for Chris to choose (native pickers + real 20px dot previews + closest-pair ΔE flag + a selectable hex box, since the sandbox blocks the clipboard API). **Caveat noted for Chris:** changing `PATTERN_COLORS` does NOT recolour existing groups (their hex is stored) — re-pick via the dot popover; a few of his picks are pale (`#C2DFFF`,`#F4CDE0`,`#E8F86D`) and may look faint as dots. **(4) ⚠️ WOD-overwrite data-loss lesson (no code):** Chris copied a workout OVER a published single-session WOD created the same day. `handleCopyWOD` ([useWODOperations.ts:781-812](hooks/coach/useWODOperations.ts#L781-L812)) inserts a NEW wod + repoints the session, then **hard-deletes the old wod once it has no remaining session references** (+ cleanup-results). Single-session → orphaned → deleted; created-today → not in the nightly backup (which predates it). Verified via service-role script: only ONE wod for that date/slot survived (the pasted copy). **Unrecoverable** except via Supabase PITR; Chris recreated it manually. Possible future guard: confirm-before-overwrite, or soft-delete/undo on copy-over of a published WOD. See ⚡ Next Session Kickoff.) -->

<!-- (spacer) -->

<!-- Older S403: 2026-08-14 (Session 403 — Opus 4.8. **Planner power-tools batch + per-lift rep-scheme presets + per-section monospace table toggle. 13 commits, all pushed, tsc+build clean, no DB-data risk. Every item coach-facing and confirmed working live by Chris as built — no prod-verification carry-over from S403.** **(1) Planner last-5-unique-workouts popovers** (`7a5c8de`, name-fix `402be07`): click an exercise chip — in a past-dot detail panel, an expanded-group chip list, or the Movement Patterns chips — to see the last 5 UNIQUE workouts it appeared in (deduped by `workout_name`, most-recent first; shared `lastUniqueWorkouts` helper). Matched by NAME not exercise id: two "Good Morning" records (`barbell-good-morning` + `good-morning`) meant the pattern-linked id missed the frequency map → "No history" on a chip that was actually programmed. Fix: `getExerciseFrequency` now exposes `display_name`; history map keyed by lowercased name+display_name (mirrors how the coverage dot matches). **(2) Chip staleness from FULL history + new colour bands** (`56d91ff`): chips were coloured off `computePatternGaps` (only ~16wk lookback near today) so months-old exercises wrongly read grey "Never"; now sourced from the full-history map (loaded eagerly on Planner mount). Bands: ≤14 green / 15–28 yellow / 29–60 orange / 61–90 red / 90+ light-grey / **Never = faded dark-grey (retire cue)**. **(3) Movement Patterns drag-to-move + multi-select** (`7f6c097`, `3352103`, copy `a85bfc2`): drag a chip's handle onto another group's row to move it; a **Select** mode ticks several then a **Move|Copy** toggle + "…to [group]" applies to all (Move removes from source, Copy keeps — patterns may share exercises). Plus per-group **sort toggle** recent↔stale/never-first (`c329ef1`). Junction tables only (`movement_pattern_exercises`) — bounded config, no growth/scaling risk. **(4) Collapsible Planning Grid** (`856f6ed`): chevron hides the grid + its date-window/RM-filter controls; persisted `localStorage 'planner-grid-open'`. **(5) Adults session sub-filter** (`6fe0fa6`): All / WOD / Foundations toggle (Foundations = Foundations + Foundations/WOD); hidden on Kids (single session type). Drives the existing `excludeSessionTypes`. **(6) Per-lift rep-scheme presets** (`439650e`): Configure Sets/Reps modal → save + name a lift's scheme (constant / variable / RM test), re-apply in one click, per-lift chips. New table **`lift_rep_scheme_presets`** (RLS per user; upsert on `user_id,lift_id,name`) — **Chris ran the SQL, confirmed working**. Hook `lib/lift-preset-storage.ts`. **(7) Per-section "Keep table layout (monospace)"** (`f50aa01`, preview `401f1db`): opt-in `section.monospace` flag renders that section's `content` in `font-mono` on ALL display surfaces (coach calendar, TV `/tv`, athlete app, logbook, leaderboard, publish/search previews) so a pasted strength-scheme table keeps its columns. Off by default → existing workouts unchanged. Field added to every section type variant (types/movements, useWorkoutModal, logbook-utils, 2 athlete-local). Editor textarea is already mono so added a **live display-font preview** under the checkbox (shown when content looks tabular or the box is on). For Chris's Claude-Desktop tables shown on the gym screen during workouts — not athlete-facing. Docs: Planner "How it works" modal + Forge-Feature-Overview updated.) -->

<!-- Older S402: 2026-08-10 (Session 402 — Opus 4.8. **4 shipped fixes + Whiteboard Week 32.1 "The Ghost" + 2 magic links. All pushed, tsc+build clean, parity clean (844).** **(1) Private session leaked onto athlete Leaderboard** (`e64c464`): [LeaderboardView.tsx](components/athlete/LeaderboardView.tsx) `loadWods` inner-joined `weekly_sessions` but never filtered `is_private` (S399 gated booking page but missed the leaderboard) → added `.neq('weekly_sessions.is_private', true)` to the embedded join. Chris confirmed fixed. Secondary pooling paths (sibling ±60d grouping + benchmark fetch-all-wods) left alone — only leak if a private event reuses a public `workout_name`/benchmark, which special events don't. **(2+3) Mobile WOD editor** (`cb573c0`+`8b3d9b0`): note "Library should stay at top when scrolling sections" — root cause was the **on-screen keyboard** (S379 class), not a missing sticky. Editor = `isPanel` mode (always, `app/coach/page.tsx`); its header is already `sticky -top-6` but the panel was `h-[calc(100vh-72px)]` — `100vh`=LAYOUT viewport, keyboard doesn't shrink it, so tapping a section's text field overflowed the fixed panel and the browser scrolled the sticky bar off-top. Fix: size panel to `window.visualViewport` on mobile (<1024, MovementLibraryPopup pattern) + `min-h-0` on the form. Follow-up: made it **full-screen on mobile** (fills VV, no 72px nav gap) + `overflow-x-hidden` on panel+form to kill the **horizontal pan on Mimi's iPhone**. Desktop unchanged. **Chris to test both phones.** **(4) display_name/name divergence on rename** (`8212682`): Katja Schmidt — renamed primary Luisa→Katja + child→"Luisa Schmidt" but "Viewing as"/Book filters showed old names. `members` has BOTH `name`+`display_name`, UI renders `display_name || name`; **add-family sets both equal but the two EDIT paths each wrote only ONE** ([AthletePageProfileTab.tsx:212](components/athlete/AthletePageProfileTab.tsx#L212) name-only; [book/page.tsx:464](app/member/book/page.tsx#L464) display_name-only). Chris's direct `name` edit left `display_name` stale. Data-fixed 2 rows (`display_name=name`) + both edit paths now write both. **(5) Whiteboard 32.1 "The Ghost"** (`f33363c`): 2026-08-07 metcon 6rd Rower/Burpee/DUs, middle board only. Score section `section-1765486851260` = `reps`+`scaling` only → `reps_result` = **Cals+Burpees+DUs SUM** + scaling Rx/Sc1/Sc2. **14 WSR, no lift_records** (metcon). 09:00 (4: Städele/Mimi/Irene Rx, Aline Sc2), 17:15 (10 incl **Justine DNF**, Wayne 623, Christian 550…). **Julia Weihe skipped** (DUs "!" illegible, Chris: leave it). Session split via `bookings`; both sections already published. Script [enter-week32-1-ghost.ts](scripts/enter-week32-1-ghost.ts). **Chris to verify 17:15 modal.** Magic links: katja.schmidt-moehlenkamp@gmx.de ×2.) -->

<!-- Older S401: 2026-07-30 (Session 401 — Opus 4.8. **Whiteboard Week 31.1 + 31.3 entered (68 WSR + 14 lift_records across 7 sessions) + 1 score-entry fix + magic link. All pushed, tsc+build clean, parity clean (833).** **(1) Week 31.1** (`8d5a292`): Board A KB-Clean metcon (27.7 10:00, 8 athletes) + Board B "M-up/T2B/WW/FS/Burpee" metcon across 27.7 17:15+18:30, 28.7 18:30, 29.7 09:30 (31 rows). **39 WSR, no lift_records** (metcons). Column→field map read from each section's `content` annotations (Load 1 / Scaling 1-3 / Trk). FS load derived by tier+gender (Rx 34/52 … Sc3 15/30 W/M); Board-A KB single-bell kg; track Run=1/AB=2; ChristianM FS `modified` "Heels raised on wedge". **The right-hand board list spanned 3 sessions** — resolved every name via `bookings` (the `→` arrow on the board marks where the 28.7 group starts). Chris booked 2 unbooked Board-A names (Dani/Mimi) before I wrote. **(2) Week 31.3** (`e4661e2`): OHS Testing 10RM + 30-20-10 metcon (Push-up/HPC/GHDHE/DUs), 29.7 17:15+18:30. **29 WSR + 14 lift_records** (Overhead Squat 10RM). **Enabled `scaling_2` (Double-Unders) on the metcon section of both wods** (was off → DU column couldn't display; Chris ok'd). HPC load by tier+gender (Rx 34/43 … Sc3=10 per Chris); Martina metcon-only (no OHS, no lift_record); ChristianM/ThomasG/Senol OHS `modified` "Heels raised on wedge". **(3) FIX — score-entry stops parsing Whiteboard-Intro text as attendees** (`4618f93`): [score-entry/[sessionId]/route.ts](app/api/score-entry/[sessionId]/route.ts) block **5b** comma-split the WOD's "Whiteboard Intro" section and turned every non-booked fragment into a whiteboard-only score row (`whiteboard_name`) → intro prose became bogus attendees in the results modal. **Removed 5b**; the modal now lists booked members + explicit **trial/drop-in** name lists only (Chris: no more whiteboard names going forward; trials/drop-ins deliberately kept). Existing `whiteboard_name` rows in the DB untouched (still on leaderboards). **(4)** 31.2 (DL 5RM) + 31.3-adjacent boards Chris is entering manually; magic link herrmann_c@gmx.net.) -->

<!-- Older S400: 2026-07-29 (Session 400 — Opus 4.8. **3 shipped fixes + a whiteboard checkpoint Chris finished manually. All pushed, tsc+build clean.** **(1) NEW FEATURE — Restore ("uncancel") a session** (`84af09c`): Mimi accidentally cancelled a session with no way back. Cancel is destructive on two tables (`weekly_sessions.status='cancelled'` + all confirmed/waitlist bookings→`cancelled`, the *same* status as an athlete self-cancel). Fix records prior state so restore is exact: migration `20260723000000_add_pre_cancel_status.sql` (**Chris ran it**) adds `pre_cancel_status` to `weekly_sessions` + `bookings`; `handleCancelSession` tags the session + each flipped booking's prior status (self-cancels stay NULL); new `handleRestoreSession` + green **Restore Session** button in the session modal (only when cancelled) reopens to prior status and restores only the tagged bookings. Mimi's own session pre-dated the columns → she re-added athletes manually. **(2) FIX — Auth self-heal** (`9ad3457`): "frozen at signing in" twice in a week = `@supabase/ssr` refresh-token rotation race (app open across many tabs → concurrent requests during the hourly refresh reject the rotated token → session revoked → corrupted cookie; client still thinks it's logged in). **No auth code changed since April** — usage-pattern, not a regression. `middleware.ts` now clears stale `sb-*-auth-token` cookies + redirects to clean login on a *definitively* invalid session (getUser 4xx, NOT a network blip — guarded so a hiccup never logs a healthy user out); PKCE code-verifier left intact. Automatic recovery, no more manual clear-site-data. **(3) FIX — Shared family 10-card on sharer profiles** (`a47cf9f`): Miriam Jacht (WP) holds a card for her 3 kids; the holder counter was right (trigger aggregates the family) but each kid's own profile showed a stuck `1/10` (trigger never maintains a sharer's own counter → orphaned stale data). `useMemberData` now attaches `shared_card_used/total/holder_name` + `own_ten_card_used` per sharer; `MemberCard` renders a sharer info-chip `used/total · used N` (not a button). Scoped 3-row cleanup cleared the Jacht kids' orphan counters (backup taken; rollback = total10/used1/offset0). Chris recalced Miriam 5→6 (one Anton booking hadn't flagged `ten_card_consumed`). **(4) Whiteboard 30.4:** read + booking-cross-checked, but AnnaKr's missing metcon score put my column read one line off → **Chris entered the metcon + Sumo manually**; no script. **(5) Per-day booking limit** = `booking_rules.max_bookings_per_day` (was 1, server-enforced) — Chris changed it himself in Admin. Magic link: maria.kunkel85@web.de.) -->

<!-- S401 note: whiteboard boards were Chris-verified live pre-write; the 3 shipped items still want a prod spot-check — see ⚡ Next Session Kickoff. -->

<!-- Older S399: 2026-07-22 (Session 399 — Opus 4.8. **2 whiteboard boards (29.4 + 30.1) + a NEW "Private event" feature + protocol/rule hardening. All pushed, tsc+build clean, parity clean (779).** **(1) Week 29.4** (`c657b47`): MetCon 3×5-min AMRAP, 16 athletes across 10:00+11:00 Sun 19 Jul, 47 WSR. **Bug I introduced then fixed** (`3aad073`): wrote WSR with **bare** section_id, but the app/leaderboard require `` `${sectionId}-content-0` `` ([save/route.ts:215](app/api/score-entry/save/route.ts#L215)) — bare rows show in the coach modal but are **invisible on the athlete leaderboard**. Converted the 33 bare rows + deleted 14 dups from Chris's app re-save; protocol doc now flags it hard. **(2) Week 30.1** (`97e8898`): Bench Press 5RM (WSR + lift_records) + a metcon, 14 athletes 17:15+18:30 Mon 20 Jul, 28 WSR + 14 lift_records. Chris added a 2nd scaling field mid-task (`scaling`=pull-up band tier, `scaling_2`=barbell %bw tier via his Rx≥50/Sc1≥44/Sc2≥40/Sc3≥30 map; `load` turned off → barbell kg not stored; `reps`=barbell+pull-ups **summed**). Two gotchas: **a WOD's `workout_id` changes when Chris edits it** (18:30's changed mid-task — re-read live before writing), and **dedupe WSR on `user_id` not `member_id`** (Lena self-entered with `member_id` NULL → member_id dedupe missed it → unique-constraint crash; also selecting a non-existent column returns `data:null`+error, not "empty"). **(3) NEW FEATURE — Private event toggle** (`626d976` + fixes `fd803b6`/`48c7520`/`f9dc85c`/`99b3eab`): new `weekly_sessions.is_private` (Chris ran SQL). One **Private** button in the session modal = hides from athletes AND excludes the session's exercises from **every** coach discovery surface (Workouts movement search + free-text box + filter list, Planner, movement tracking grid, toolkit, frequency + facet counts — 8 read paths gated). Workout **name stays searchable** + purple "Private" calendar badge. Added a **"Private events"** filter (under the search box, always visible) to list all private sessions. Made athlete-invisibility **durable regardless of `status`** (booking-page query + booking-create endpoint gated on `is_private`; publish route keeps private sessions `draft`) — publishing had been re-exposing them. For special events / non-WOD sessions. **(4) Docs/rules:** whiteboard protocol gained the `publish_sections` step (`becf2a1`) + the section_id `-content-0` rule; `claude-rules.md` gained "always check Supabase `.error` — `data:null`≠empty" (`3c1ad76`); new [Stripe monthly reconciliation guide](Chris Notes/Forge app documentation/Stripe monthly reconciliation guide.md); **parallel sessions use offset times** (17:10/17:15, never identical) confirmed safe by code-check — coach WOD edit resolves sessions by date+time in 4 spots — and saved to memory.) -->

<!-- Older S398: 2026-07-18 (Session 398 — Opus 4.8. **All of Week 29 whiteboard entered (3 boards / 8 sessions / 129 WSR + 20 lift_records) + URGENT coach-tab-freeze diagnosis + 2 S397 carry-overs closed + stale memory cleared. All pushed, build clean, parity clean (765).** **(1) Week 29.1** (`ca6037f`): C&J 3RM + 3 metcons, 6 sessions, 57 WSR + 20 lift_records ([enter-week29-whiteboard.ts](scripts/enter-week29-whiteboard.ts)). **(2) Week 29.2** (`bf376d0`): 3-part AMRAP (Rings/HS/Plate), 15 Jul 17:15+18:30, 51 WSR — surfaced the **`publish_sections` landmine**: direct-DB WSR writes bypass the score-entry save route's auto-publish, so scored sections stay **HIDDEN** in the coach results modal + athlete leaderboard even though the data is saved (Pt.1 vanished on 17:15 only). Fixed by re-publishing the section (toggle off/on); **hardened the 29.2 + 29.3 scripts to auto-add scored sections to `publish_sections`**. Also: Box Over stored as gender load (6F/9M); board columns were in a different order than the WOD parts (remapped). **(3) Week 29.3** (`daf3a51`): 3 sessions 17-18 Jul incl Endurance/Hyrox, 21 WSR, metcon-only. Board-vs-booking reconciliation lesson: **check `is_og`** (Carole was OG, not a no-show — my booking query only filtered `status=confirmed`). Chris enabled `rounds_reps` on the Weekend-WOD section mid-task; Hyrox Rx loads = men heavier (WB 9/6, Lunge 30/20). **(4) Coach-page tab freeze** (URGENT, no code change): every tab but Workouts dead, sub-routes bounced to `/login`. Root cause = corrupted **server-side Supabase SSR auth cookie** — client session still valid so `/coach` rendered (client-cached), but `middleware.ts` `getUser()` saw no cookie user → redirected every real navigation to login. NOT VPN, NOT data, NOT my writes (the 406 on `weekly_sessions` was a red-herring RLS/row-count quirk). Fix = clear site data + re-login; **Incognito confirmed the app was healthy**. Tell for next time: `/coach` works but tabs/sub-pages bounce to `/login`. **(5) S397 carry-overs CLOSED**: 2 hand-entered morning sessions (2026-02-20 10:00 FS + 2026-02-25 09:30 Bench) confirmed persisted (sweep clean); restored FS Wk8.2 / Bench Wk9.1 metcon scaling eyeballed OK — Pascal Evghenia's apparent 3RM/1RM inversion was a raw-dump row-ordering artifact, DB is correct. **(6) Stale memory cleared**: whiteboard-duplicates bug (S251) — scanned 58 orphan whiteboard rows, **0 duplicate a registered athlete**; deleted `project_whiteboard_duplicates.md` + its MEMORY.md pointer (root cause fixed S394).) -->

<!-- S399 note: no verification carry-over from whiteboard entry (both boards Chris-verified live). The Private-event feature DOES want a prod spot-check — see ⚡ Next Session Kickoff. -->


<!-- Older S397: 2026-07-15 (Session 397 — Opus 4.8. **1 new feature + 1 bug fix + backup-gap recovery + Sergej lift_record + full prod spot-check backlog cleared. All committed+pushed, tsc+build clean, parity clean.** **(1) NEW FEATURE — mute athlete selection (`8dce682`):** Workouts → Athletes filter gets a **mute/unmute** toggle beside "clear" — keeps the selected list but stops it filtering results; persisted (`coach_selected_athletes_muted`); the data hook receives `[]` while muted, and the Results panel / filter chips / "Not done by selected" button all treat a muted selection as inactive (derived `athletesFilterActive` in [SearchPanel.tsx](components/coach/SearchPanel.tsx); gated in [app/coach/page.tsx](app/coach/page.tsx)). **(2) 🐛 Backup pagination FIXED (`dffc6fb`):** [backup-critical-data.ts](scripts/backup-critical-data.ts) did an unpaginated `.select()` → every table >1000 rows silently capped, so large-table backups were incomplete/unusable. Now `.range()`-paginates per table; verified bookings 3543, wod_section_results 2867, lift_records 2119 captured in full. **(3) Backup-gap RM-loss audit + recovery ([sweep](scripts/sweep-backup-gap-rm-losses.ts), [restore](scripts/restore-wk8-9-backup-gap.ts)):** swept 23 RM-testing WODs in the 2025-12-09→2026-03-19 gap (booked-but-unscored AND no surviving lift_record = genuine loss). 2 real losses restored from whiteboard photos — **Front Squat Wk8.2 (2026-02-18 17:15+18:30)** + **Bench Press Wk9.1 (2026-02-23 17:15+18:30)**, 25 athletes, 71 WSR + 48 lift_records, INSERT-only/deduped/parity-clean. Metcon scaling derived + flagged for Chris to eyeball (FS "Rx"=gender presc 25/35·20/30·15/25; Bench KB="Rx" left null, PS pistol tier=scaling_level, "Sc1-2"→Sc2). Morning sessions 2026-02-20 10:00 + 2026-02-25 09:30 = Chris did manually (⚠️ sweep still showed them unscored after — verify they saved, S356/S371 silent-save class). Non-losses: Feb-18 Michael Junkes (no-show), Feb-24 Leah/Justine (did WOD day before), Feb-27 Kids. **(4) Sergej Felsing Deadlift 5RM 100kg lift_record rebuilt ([script](scripts/rebuild-sergej-deadlift-liftrecord.ts)):** was on the leaderboard (WSR) but missing from his Lifts view; parity now fully clean (745 checked). **(5) Full prod spot-check backlog CLEARED (Chris verified):** all S396/S395/S392/S390/S384/S383/S381 items confirmed working — best-of-repeat leaderboard (Teemu Wk28), Parked sub-toggle, instant modified-flag tooltip, 4 whiteboard boards, modified-movement flag, Parked leak, paste-to-empty=Hidden, Sergej/Luisa cleanups, Wellpass status chip, "Not done by selected" chip + Planner recency, Custom Movements search, movement extraction, Hide-survives-edit, demo-bar, Drop-ins/Hide/5-min increments, Birthdays banner + login modal + RM renames.) -->
<!-- S398 note: prod spot-check backlog was already cleared in S397; S398 added no new app code (whiteboard data entry + scripts + cookie diagnosis), so no new verification carry-over. -->

<!-- Older S396: 2026-07-13 (Session 396 — Opus 4.8. **Whiteboard score entry (4 boards, 79 rows) + 4 code fixes + 3 magic links. All committed+pushed, tsc+build clean.** **(1) S395 carry fixes (`518ec36`):** added **Block** button to the Parked tab (block route now clears `parked=true` so a blocked member leaves Parked cleanly); replaced the modified-flag leaderboard tooltip's native `title` (browser ~2s delay) with an instant CSS group-hover tooltip. **(2) Whiteboard: Week 28.2** Swiss Ball/T2B AMRAP, 30 rows (`88aaa50`) — deleted 3 orphan (no-member/no-name) rows on the 06 Jul 17:15 session, Freddy drop-in; **added crossed-7 rule to the protocol** (Chris's 7 is always crossed → an uncrossed top digit = 1, not 7). **(3) Whiteboard: Week 28.3** MetCon/Carry/Pistol, 26 rows across 3 sessions (`dd775f6`) — Cals = SkiErg+Rower summed, reps = pistol count (**Chris added a reps field to the wod mid-task**), scaling = pistol tier, adapted text after scaling → `modified_note`; Sabrina Reichle as trial whiteboard row. Follow-up (`47f6a7e`): the "Sks" note actually meant **5kg** (6 notes corrected). (`d8f8b39`) Leaderboard tooltip drops redundant "— see whiteboard" when a modified note exists. **(4) Whiteboard: Weekend WOD #26.14 (29.05.26)** from board 22.1 — 13 athletes across the 09:00+17:15 copies (`9560f72`): Pendlay Row "WOD Pt.1" load (total of last 3 sets) + metcon (Rounds → a field Chris added, Dips/GHD/HS → Scaling 1/2/3 with his Dips rule, KB "LB" → load, Rx filled 16/24 by gender). Foundations 18:30 from board 22.3 — 11 rows (`abfa28d`): GHD from the "AbSit-up ✓" column (Niko=Rx, others=Sc3), Dips left blank, Sandra Lederle skipped (blocked). **(5) UI (`fb8cdcb`):** moved the **Parked** tab into the Pending/Blocked sub-toggle (removed the standalone top-level tab). **(6) Whiteboard: Week 28.6** MetCon/Carry/Pistol, 12 rows (`964877e`) — same as 28.3 plus a new **AB (AirBike) column**: Cals = Ski+Row+AirBike summed (Chris confirmed). **(7) Leaderboard best-of-repeat (`10adb00`):** `bestResultPerUser` only ran for cross-week grouping, so an athlete who repeated a WOD within a week (Teemu, Week 28) showed twice; now always kept-best before ranking (no-op for single results). Magic links: siebertfabian8@gmail.com, nikolina.vlasalija@gmail.com, herrmann_c@gmx.net.) -->

<!-- (spot-checks below now verified/cleared S397 — kept for reference until trimmed) -->

<!-- Older S395: 2026-07-07 (Session 395 — Opus 4.8. Whiteboard/duplicate cleanups (mostly DB-only) + 2 code fixes + 1 new feature. (1) Sergej Felsing whiteboard→profile link (DB-only): S394 approve-migration relinks only the ONE picked `whiteboard_name`, but Sergej had TWO identities (member "Sergej" vs full "Sergej Felsing" Week-27 trial) — relabeled 5 WSR rows, cleaned 8 intros, booked profile into 8 sessions. (2) Delete-names mishap (DB-only): misread "delete names" → DB-deleted 10 unlinked whiteboard SCORE rows + edited 10 intros; restored intros from backup, deletes scoped `member_id IS NULL` so no profile score touched. (3) 🐛 Backup script truncates at 1000 rows — unpaginated `.select()`; not reliable for large tables; fix = paginate. (4) Parked leak (`bb0cd27`): Workouts athlete filter + `/coach/athletes` didn't exclude parked; added `.neq('parked',true)`/`!m.parked`. (5) Luisa Schmidt duplicate merge (DB-only): merged card+DOB+bookings onto mum-login record, stripped Paul's family-member card. (6) Paste-to-empty-slot = Hidden (`86c6aad`): `handleCopyWOD` empty-slot sessions now `status='draft'`. (7) NEW FEATURE — 'Modified movement' flag (`292ee08`): per-athlete `modified` bool + `modified_note` on `wod_section_results`; coach score-entry red `!` toggle + note, leaderboard red `!` + tooltip, ranking ignores it. Built following the `dnf` pattern.) -->

<!-- Older S394: 2026-07-06 (Session 394 — Opus 4.8. **Audit Pass 4 (the parked Fable-5 candidate — run as a Fable subagent, coordinated on Opus) + 4 UI/graph fixes + Week 27 whiteboard entry. All committed + pushed; build clean.** **(1) Audit Pass 4 — cross-cutting reasoning (`5ec0f43`,`8c2d075`):** Fable-5 subagent found 8 silent-failure sites, all fixed: **members/approve** whiteboard-score migration was one all-or-nothing bulk UPDATE — a single unique-constraint collision (member already self-entered a score for the same session) failed every row, error only logged, approval still returned success → the **whiteboard-duplicates bug**. Now migrates what it can + warns on the rest. **useWODOperations section-removal** (WSR/lift deletes, rename-migration, scoring-field clears) ran on the coach browser token → 0 rows under athlete RLS (S344 ghost-score class); routed through new service-role `/api/sessions/edit-section-results`, failure now blocks the save. **score-entry/save** partial WSR failures now surfaced client-side (S371 recurrence — client only read `res.ok`) + lift/benchmark cascade errors captured. **Stripe webhook** `invoice as any`→resilient subscription-id resolver (S358 field-relocation class); 10-card activation + subscription-cancel writes now throw→Stripe retries (were 200-on-failure = money/access drift). **publish-workout** reports `bookingSessionReady`. **(2) 'Clean' Oly-lift filter mis-credit:** NOT a code bug — a Kids prose note "planned to do Clean but had to abandon" made the S384 extractor credit the bare word "Clean" on 12.06. Chris reworded to "BBL CLN"; verified 12.06 no longer hits (acronyms only expand on structured lift slots, never prose). **(3) Movement Tracking grid (`69c9309`):** athlete-name column frozen (`sticky left-0`) so only exercises scroll. **(4) Lifts graph (`9792a62`,`e42b774`):** x-axis was categorical (3 RM tests in a year looked a year apart) → numeric `ts` time-axis + `interval={0}` so every date label shows. **(5) Week 27 whiteboard (`4d38de0`):** DL Testing 03/07 09:00 (5 new athletes — Städele/Özdilek/von Rüden/Koffler/Hiles — 25 WSR + 5 Deadlift 5RM lift_records; rest of board was 01/07, already entered), Annie 05/07 10:00 (10 WSR + 10 benchmark_results; Gloria Stoffer AB/DNF skipped), 27.3 Clean/Run 17:15+18:30 (13 WSR, time+track+scaling after Chris switched section load→scale; Miriam Jacht + Patrik Gruber DNF). Parity OK (678 RM results). Added band→scale + "ok = full hold cap" legend to the whiteboard protocol.) -->

<!-- Older S393: 2026-07-03 (Session 393 — Opus 4.8. **Security + data-integrity audit (passes 1–3 of 4) + a Workouts-filter mystery.** Session opened as a Fable-5 advisory chat — the pricing/usage-limit/export-control claims in Chris's pasted transcript were unverifiable and flagged as likely fabricated; landed on: run the mechanical audit on Opus, reserve Fable 5 for pass 4. Scope = **Security + Data-integrity only** (perf/architecture deliberately out). **7 fixes shipped, all tsc+build clean:** **(1) Auth (`45a3acf`):** reviewed 71 routes + 73 service-role sites; `cron/expire-memberships` now fail-closed (was fail-open if `CRON_SECRET` unset — Chris confirmed it's set); `stripe/create-checkout` now guards `memberId` (self or owned family member only — parents-pay-for-kids preserved) — was a MEDIUM IDOR trusting the request body; `achievements/athlete-records` → `requireCoach` (its only caller is the coach award modal). Deferred (Chris's call): `members/check-status` open email-enumeration (LOW). **(2) Data-loss (`16de732`):** paginated the coach-calendar `weekly_sessions` fetch (`useCoachData:85`, unbounded — latent, 402/1000 today) + the unlinked-whiteboard-names scan (`useMemberData:27`, likely already truncating over 2,448 WSR rows); added `berlinToday()` to `lib/bookingRules.ts` and pointed the 6 DB-persisting date defaults at it (award/benchmark-results/movement-results — `.toISOString().split('T')[0]` gives the UTC date on the Vercel server, mis-dating records logged in the ~2h after Berlin midnight). **(3) Rule fix (`26ed7e1`):** `claude-rules.md` now splits browser `formatDate` vs server `berlinToday` (formatDate is a no-op on the UTC server — the trap I nearly hit). **(4) Kickoff note (`9f6ee75`):** pass 4 (cross-cutting reasoning) parked as the Fable-5 test-drive candidate + quarterly-scan reminder added (next due ~2026-10-03). **(5) Bodo Lehmann "missing from Workouts athlete filter" (`f8e76ad`):** NOT a permission/status bug — his `date_of_birth` was `2026-03-09` (current year), and `SearchPanel`'s age-split files anyone computing <16 under **Kids**, so a 13-visit adult sat in the Kids list. Same current-year typo hit **Aliona** + **Emilia Peresyov** (both attended). Root cause: athletes enter their own DOB and the mobile year dropdown listed the current year at the top → mistap. Fix: block current-year/future DOB on the athlete profile form (desktop `max`, mobile year list starts last year, save-handler reject). Youngest genuine member is born 2023, so real Eltern-Kind-Turnen toddlers (~1+, born prior years) are never blocked. Chris corrected the 2 kids; will fix Bodo's DOB tonight.) -->

<!-- Older S392: 2026-06-29 (Session 392 — Opus 4.8. **(1) Wellpass status badge split** (`435771c`): red **"blocked"** now means actually booking-restricted (any linked member), amber **"review"** = scoring flagged but not yet actioned — was conflating the `shouldBlock` *suggestion* with enforcement. **(2) Clickable status chip → household triage** (`b258619`): the status chip is now a dropdown (ok/review/blocked/paused, current state checkmarked). New column **`wellpass_identities.review_cleared`** (migration `20260628000000`, **Chris ran the SQL**) — ok=unblock all+acknowledge, blocked=block all+resolve, review=unblock+re-flag, paused=pause; per-member Lock/Block buttons still there for granular control. Weekly Excel import re-arms `review_cleared=false` for every still-flagged household so chronic slackers resurface instead of staying acknowledged. tsc clean, tested by Chris. **(3) Week 26.3 whiteboard scores** (`a1cde7a`): 26.06 17:15 "KB RDL, HPS, SMC, Roll-out, Push-up" 16-min HPS AMRAP (ascending ladder), 10 athletes, WSR only (metcon, no lift_records). Snatch→weight_result (Rx W20/M30, Emily 17.5), Push-up→scaling_level, Roll-out→scaling_level_2, R+R→rounds/reps. Daniel Braatz 8+9; Carmine attended (app booking cancelled → score written, Chris re-books confirmed); Lena Jähn removed. Parity OK. Script [enter-week26-3-hps-amrap.ts](scripts/enter-week26-3-hps-amrap.ts). **(4) Stripe free-trial Q&A** (no code): trial is owned by Stripe (hardcoded 30d monthly); retrospective change = edit the trial-end date in the Stripe Dashboard (card always collected → auto-bills, webhook syncs). Chris: edge case, Dashboard route fine.) -->

<!-- Older S391: 2026-06-28 (Session 391 — Opus 4.8. Workouts-page filter UX, 3 commits, all live + tested by Chris. **(1) Filter persistence** (`2e65dc3`): **Session Types** now persists across navigation + logout (localStorage), joining **Athletes** which already did — Chris's explicit choice; the other filters (Movements/Tracks/Workout Types/Section Types/search text/not-done toggle) stay session-only. New reusable [usePersistedState.ts](hooks/usePersistedState.ts) hook (SSR-safe; generalizes the old Athletes-only pattern, key `coach_selected_athletes` unchanged so existing selections carry over). **(2) Active-section highlight** (`2e65dc3`): every sidebar filter `<summary>` header in [SearchPanel.tsx](components/coach/SearchPanel.tsx) gets a teal accent (`bg-[#178da6]/10 text-[#178da6] border-l-2`) + `(count)` badge when it has a selection, so a lingering filter is obvious when the page reopens; added a **clear** link to Session Types too. **(3) NEW FEATURE — saveable Session Type groups** (`6b87f47`): named bundles like Adults / Kids, mirroring Custom Movements groups. New Supabase table **`session_type_groups`** (user_id, name, session_types text[], display_order; RLS auth.uid()=user_id — **Chris ran the SQL**) + hook [session-type-storage.ts](lib/session-type-storage.ts) `useSessionTypeGroups` (create/rename/delete/updateGroupSessionTypes; no stored `active` — a group is active when its types == current selection). UI = chips in the Session Types section: "+ Save as Group" saves current selection; clicking a chip is **replace mode** (shows only that group, click again to clear); hover ✎ → rename / Edit session types / Delete. DB-backed so groups sync across Chris's two machines. tsc clean throughout.) -->

<!-- Older S390: 2026-06-28 (Session 390 — Opus 4.8. **(1) Planner accuracy** ([PlannerSection.tsx](components/coach/analysis/PlannerSection.tsx) + [movement-analytics.ts](utils/movement-analytics.ts), `3952f58`): exercise-chip recency lookback was fixed at 16wk while the grid spans the visible window → a workout older than 16wk showed a grid check but a grey "Never" chip; chip lookback now grows to the leftmost visible week. Also paginated `fetchPublishedWorkouts` (was silently capped at PostgREST's 1000 rows → dropped oldest sessions on long scroll-back). **(2) Whiteboard score entry via protocol** — Week 26.2 (24.06 TGU metcon, 15 athletes, `82f35a8`), 25.2 (17.06 RDL 5RM + KB metcon, 18, `c56d960`), 25.1 (16.06 18:30 + 17.06 09:30, 16 incl Bonnie as whiteboard drop-in + AnnaKr booking flipped late_cancel→confirmed & trial cleared, `42b0bc6`). Added **gender-split KB scaling tiers** (Rx/Sc1/Sc2/Sc3 from `16/24·12/20·8/16`, read `members.gender`, in-between weight takes the lower tier) across ALL sessions of the RDL+KB workout so the pooled leaderboard ranks within tier. Parity OK. **(3) Protocol doc** (`227186c`/`f31e8ca`): capital surname-initial rule (cross-check vs booking), scaling-tier+load convention, stop-and-ask on blank gender. AnnaKr alias added to booking list; Chris filled the 14 blank-gender members. **(4) Mobile whiteboard close-button fix** (`895fa7f`): coach gallery lightbox X was `-top-12` (off-screen on mobile) → moved inside image `top-2 right-2`. Verified by Chris. **(5) NEW FEATURE — "Not done by selected" chip** (Workouts Results header, `3793eb8`): given selected athletes, shows only workouts NONE attended; flips the existing member filter to search-all + exclude attended names; new [/api/coach/attended-workouts](app/api/coach/attended-workouts/route.ts) (service-role, paginated). Chris: "looks good, can tweak later".) -->

<!-- Older S389: 2026-06-25 (Session 389 — Opus 4.8. **(1) Cash-grace banner VERIFIED on prod + polish.** S388's [PaymentDueBanner.tsx](components/athlete/PaymentDueBanner.tsx) confirmed live via magic link: cash member (lisa.paval) saw it; Stripe payer (marion.sontheimer) + trial (pkpeterkroll0807) correctly saw nothing. Styling per Chris — solid amber bg, larger bold text, and the 🙂 in a small white circular badge (yellow emoji was lost on amber). `LEAD_DAYS` temporarily bumped to 4 to test a member with 3 days left, then reverted to 2. **(2) Kids 10-Card (€85) — new athlete-app purchase option.** Mirrors the adult 10-card (10 sessions, 12-month validity) at a lower kids price. New `'10card_kids'` ProductType in [lib/stripe.ts](lib/stripe.ts) → env `STRIPE_PRICE_10CARD_KIDS_ID` (price `price_1TmEWhRc17SxAgW5WHn0yu46`); [webhook](app/api/stripe/webhook/route.ts) activates the same card for both; [create-checkout](app/api/stripe/create-checkout/route.ts) needed no change (one-time, no tier gating). [AthletePagePaymentTab.tsx](components/athlete/AthletePagePaymentTab.tsx) now shows both cards in a 2-up grid; kids card is white like the adult but with a pink "Buy Kids 10-Card" button. **(3) "All plans include" expanded 4→11 features** (added: workout timer, 1RM/rep-max calculator, automatic PR detection, whiteboard photo logging, personal activity tracker, shareable result cards, push notifications) — all sourced from the verified Forge-Feature-Overview doc. tsc + `npm run build` clean.) -->

<!-- Older S388: 2026-06-25 (Session 388 — Opus 4.8. **(1) Cash-payment grace + reminder banner (bug fix).** Cash athlete-app subscribers were cut off the instant payment slipped — the coach-page auto-expire in [useMemberData.ts](hooks/coach/useMemberData.ts) flips `active→expired` once `athlete_subscription_end < now` (Stripe excluded). Now: **4-day grace** for cash `active` subs (trials still expire on their end date), plus a new dismissable **[PaymentDueBanner.tsx](components/athlete/PaymentDueBanner.tsx)** on the athlete app + `/member/book` showing *"Mitgliedschaft fällig… noch X Tage"* from **2 days before end through the 4-day grace**. Never shows for Stripe members (checks the `subscriptions` table — S380 don't-nag-auto-renew rule). The old `subscription-expiring` notification is coach-only by design, so cash athletes previously got no reminder at all. tsc clean. **(2) Empty-results audit — no losses found.** New [audit-empty-results.ts](scripts/audit-empty-results.ts) over all 2,448 WSR rows + lift parity: A=0 nulled/blank rows in scoring sections (mirrors the save route's own `isScoreEmpty`), C=0 confirmed WSR losses (none missing-from-WSR-but-present-in-benchmark/lift — the Karen signature), parity clean (601 checked). B=48 'partial' sessions = not-entered-yet + no-shows kept confirmed (recurring single names like Carole Schultz = attendance pattern, not loss). **(3) S386 verified** (Chris: all 3 prod checks pass) + **orphan-check SQL fixed** (Supabase saved query now carries the `AND ap.user_id NOT IN (SELECT id FROM members)` clause; line 37 there = doc line 52, offset by 15 lines of markdown preamble).) -->

<!-- Older S387: 2026-06-25 (Session 387 — Opus 4.8. Whiteboard-data cleanup + reusable protocol, no app code. (1) "Jenny" whiteboard name → Pascal Evghenia (`e96e39c`): booked her on all 25 sessions, removed the name from each intro, re-keyed her 4 OHP/PP rows, set `members.whiteboard_name='Jenny'`. Claudia PP-1RM already corrected to 40kg by Chris. (2) Definitive adult whiteboard-name list — diffed [Athletes booking list.md](Chris Notes/Forge app documentation/Athletes booking list.md) vs every booked member, added 12 missing adults, fixed Dinny→Braatz, excluded kids. (3) New whiteboard score-entry protocol ([memory-bank/whiteboard-score-entry-protocol.md](memory-bank/whiteboard-score-entry-protocol.md), cue "run the whiteboard protocol for [photo]") — coach results modal is the SINGLE entry point, one score fans out to WSR + lift_records, WSR-first. (4) Transcription-accuracy measures after the S386 Claudia 45-vs-40 misread + board-writing tips for Chris.) -->

<!-- Older S386: 2026-06-24 (Session 386 — Opus 4.8. **(1) Week 7 (9-11 Feb) OHP & Push Press RM-testing recovery — full restore from whiteboard photo.** Same loss family as S385: WSR weights nulled pre-March (the `load:false` edit bug) + the April historical-import reset (S313–S315) wiped `lift_records`; the **backup gap (2025-12-09 → 2026-03-19)** means NO DB backup covers this loss. Recovered from whiteboard photo "2026 Week 7.1" (stored in `whiteboard_photos`): **132 `lift_records`** + **128 `wod_section_results`** across all 5 sessions (36 athletes; OHP→`Strict Overhead Shoulder Press`, PP→`Push Press`; each value mapped to its section by lift name+`rm_test`, each athlete to their confirmed session day). Chris's own row matched the 04-23 backup exactly = transcription cross-check. Verified 11 09:30 in the coach modal BEFORE rolling out the other 4 sessions. Parity clean (593 weighted RM results). Scripts: [restore-week7-ohp-pp-liftrecords.ts](scripts/restore-week7-ohp-pp-liftrecords.ts) (`ce51375`) + [restore-week7-ohp-pp-wsr.ts](scripts/restore-week7-ohp-pp-wsr.ts) (`10aebcc`). **Outstanding (Chris manual):** Lisa B (whiteboard-only, no account) + Pascal Evghenia (attended 10 18:30 as "Jenny", unbooked → book her first). **(2) Achievements display fix + 'Prior skill' (`f20e85b`):** band-assisted bronze tiers added 19-Mar pushed earned badges up a tier, so the new bottom tier showed a misleading lock. Lower tiers below the highest-earned badge are now **claimable** (forward progression still strictly gated; only the true next-goal pulses). New **'Prior skill'** claim option (athlete came in already able) — `athlete_achievements.is_prior` column (SQL run), 'Prior' badge tag + detail-modal status. [AthletePageAchievementsTab.tsx](components/athlete/AthletePageAchievementsTab.tsx). **(3) Drop-in fix (`6217e47`):** adding a drop-in whose name matches an existing trial now MOVES it (was showing both Trial + DI chips) — [useBookingManagement.ts](hooks/coach/useBookingManagement.ts). **(4) Orphan-check:** DB clean; the "2" `orphan_athlete_profiles` was a STALE saved query missing `AND ap.user_id NOT IN (SELECT id FROM members)` (flagging family-member kids) — canonical query in `supabase-orphan-check-queries.md:52` is correct, re-copy from there. `unbooked_section_results=21` is informational. See project-history.) -->

<!-- Older S385: 2026-06-21 (Session 385 — Opus 4.8. **RM-test lift weight-loss incident — TWO code bugs fixed + data recovered.** Symptom: Back Squat Testing 3&1RM (Wk13: 23/24.03+01.04), Front Squat Testing 5RM (20-21.04), Pendlay Row 5RM (25/27.03) showed only a fraction of their results. **Bug 1 (`866bba3`):** RM-test lift sections need `scoring_fields.load=true` (the weight IS the score), but [useScoreEntry.ts](hooks/coach/useScoreEntry.ts) only set it in-memory → stored WODs sat at `load:false` → the S338 edit-cleanup in [useWODOperations.ts](hooks/coach/useWODOperations.ts) nulls `weight_result` on a load true→false flip, so editing/renaming these WODs wiped every athlete's weight. Fix: force `load:true` on save for any `rm_test` section. **Lift-records loss (real cause, confirmed late in session):** NOT an app action — the **April historical lift-import project (S313–S315)** reset `lift_records` ("DB had been reset since S314 import, only 53 records total" per the S315 log) and re-seeded from Chris's historical-PR master JSON, which lacked the March testing-day results. Confirmed athletes' bookings stayed `confirmed`, proving no booking/no-show/cancel path was involved (Chris removed nobody). **Defensive fix (`f5a3d58`):** [scoreCleanup.ts](lib/coach/scoreCleanup.ts) no longer deletes `lift_records` on booking removal — sound (a removed person has no result) but NOT the cause of this loss. **Recovery (`6afc589` tooling):** restored WSR weights by-id from 04-17 (BS) + 05-05 (FS) backups, rebuilt 70 `lift_records` (42 BS + 8 FS + 20 Pendlay) from the corrected WSR, flipped `load:true` on 14 sections, deleted 10 whiteboard dups from athletes who registered after the backup (kept Nils, whiteboard-only). Chris filled post-backup-date gaps manually. Sweep: 0 damaged sections remain. **Safeguard:** new `scripts/check-wsr-liftrecord-parity.ts` found **33 MORE silent losses** (Clean & Jerk 5RM 28.04+04.05, Bench Press, Deadlift, OHP) → all rebuilt (103 total); wired to a **monthly GitHub Action** that emails Chris on any finding. **Also this session:** rotated the exposed Supabase `service_role` key — migrated to new `sb_publishable_`/`sb_secret_` keys, legacy JWT keys disabled (see techContext). See project-history for full forensics.) -->

<!-- Older S384: 2026-06-20 (Session 384 — Opus 4.8. Workouts/Sessions bug-fix batch, 5 commits, all pushed live. **(1) Custom Movements search persists** (`6b748e1`) — [SearchPanel.tsx](components/coach/SearchPanel.tsx) no longer clears the search box after allocating an exercise to a group; filtered list reopens, added item greyed "✓ tracked" → add several "drill" matches in a row without retyping. **(2) Movement extraction rewrite** (`cd91bc7`+`dbb3503`) — movements written on a coaching/instruction line ("Add weight for sets of 1x Hang Power Snatch + 3 OHS") were dropped wholesale, so Hang Power Snatch never registered for Daniel Braatz/Carla Courtois on 15.06. Generalized [extractMovementsFromText](utils/movement-extraction.ts) to credit EVERY library movement named in a line — primary + "A or B" + "A & B" + scaling options "(Sc: Pull-Up Banded)" — longest-wins-per-overlap. Per Chris: the tracking grid/Planner/toolkit represent what the class practised/was offered, NOT per-athlete capability (per-athlete scaling lives in the results modal). Audited all 333 published workouts: misses 24→~0; the full-name scan only adds in-text movements (no spurious credits). Caveat: an uncatalogued variant ("Snatch Grip Deadlift") attributes to component movements (Snatch+Deadlift) — add it to the library and it wins as longest match (retroactive). **(3) Hide-from-athletes survives edit+save** (`f66103d`) — [useWODOperations.ts](hooks/coach/useWODOperations.ts) was forcing `weekly_sessions.status='published'` on every WOD save, wiping the Hide toggle's draft; now scoped `.neq('status','draft').neq('status','cancelled')`. **(4) Demo-bar clip matcher** (`d26f152`) — when a movement shared a line with others after reordering ("KB Dead Bug, Plank"), the purple clip area showed "Dead Bug" instead of "KB Dead Bug" + dropped the rest; [section-video-matcher.ts](utils/section-video-matcher.ts) now prefers the longest/most-specific name + surfaces every movement on the line. **Pending:** prod spot-checks.) -->

<!-- Older S383: 2026-06-18 (Session 383 — Members + Sessions UX batch, 6 commits, no carry-over bugs. **Members:** new **Parked** tab — `members.parked` boolean hides non-attenders (e.g. once-a-year Austria friends Anna Maria Bauer + Tobias Heide) from Active/At-Risk/Subscriptions/10-Card lists + their count badges; **Park** button on Active rows, **Restart** on Parked; booking/login access untouched (real `status` stays active). Merged Pending+Blocked into ONE tab with an inline toggle. Added **"Last 2 months"** (60d) attendance timeframe option. Removed the dead **DI** member-type chip. **Sessions:** **drop-ins per session** — new `weekly_sessions.drop_in_names` text[] (mirrors `trial_names`); "+ Drop-in (enter name)" in the add-member picker, counts toward capacity, shows on the Score Entry whiteboard as "Name (drop-in)" + in the calendar card Booked chip/popover; collapsible **Admin → Drop-ins panel** = date-filtered yearly tally (mirrors trial stats). **Hide-from-athletes toggle** in the session modal flips `status` published↔draft (booking page + create API already gate on published) so interim copy-to-empty-slot sessions aren't bookable; purple "Hidden" badge on the calendar card. Workout-editor time dropdown now **5-min increments** (session-mgmt modal intentionally stays 15, per Chris). 2 SQL migrations run by Chris (`members.parked`, `weekly_sessions.drop_in_names`). Confirmed the **parallel-WODs-at-offset-times** (18:30/18:35/18:40, all physically start 18:30) workflow is safe — each session fully independent. Magic link issued for kathrin.muehlen@gmx.de. **Pending:** prod spot-checks.) -->

<!-- Older S382: 2026-06-16 (Session 382 — Data-integrity diagnostics consolidation + 4 planner fixes + publish-no-notify default + Custom-Movements hover-highlight. **Data integrity:** ran the orphan/duplicate health check; cleaned 1 duplicate lift (Peter Kroll, double-tap) + 1 orphan benchmark reaction; the 90 `unbooked_section_results` was benign (78 whiteboard names + member_id-keyed scores the query ignored). Rewrote the check — `unbooked` now matches bookings under `member_id` + excludes whiteboard rows; `orphan_athlete_profiles` excludes family-member kids (member row, no auth login — it was flagging Chris's kids); added 4 checks (athlete_profiles, wellpass_identity_members, subscriptions, push_subscriptions); **merged the two diagnostics docs into one** ([supabase-orphan-check-queries.md](Chris Notes/Database & Supabase/supabase-orphan-check-queries.md)) with a hard-error vs expected-nonzero legend. **Planner:** (1) `acb25ca` restored-view coverage race — saved 12-mo view showed Dec→ columns but coverage only spanned the default ~March window until toggled; recompute effect now keyed on `[patterns, window, track]`. (2) `05b7ca3` single recompute per pattern edit. (3+4) `cf703a7` Kids leak (session-type exclude was exact-match → `Kids & Teens (7+)` slipped past) + variant mis-credit (`computePatternGaps`/`detectWeeklyCoverage` only knew pattern names → `KB Bent Over Row` substring-matched barbell `Bent Over Row`; both now seed from the full exercise library). RM pattern verified barbell-only; Workouts Custom Movements already used full library. **Publish:** `1575c4c` 'Notify athletes' defaults OFF. **Workouts:** `420e8f6` hover a Custom Movement → grid column highlights + scrolls into view. 8 commits + close. **Pending:** prod spot-checks.) -->

<!-- Older S381: 2026-06-16 (Session 381 — Planner RM-filter bug fix + RM-workout rename/calendar sync + Birthdays banner + Birthday login modal + waitlist OG/Remove. **Planner RM-only filter** missed lifts also used in a benchmark that week — truthiness bug in [pattern-analytics.ts](utils/pattern-analytics.ts): empty `{}` slug match shadowed the rmType-carrying display match in `hitName || hitDisplay`. Now `hitName?.rmType ?? hitDisplay?.rmType`. Verified across 94 historical RM tests / 11 lifts. **Renamed 8 RM workouts** to include "Testing" (data-only, 29 rows) so a "Testing" search on the Workouts page surfaces all RM workouts; **synced the 29 Google Calendar event titles** via the service account (titles are publish-time snapshots — DB rename doesn't reach them). **Birthdays banner** on `/coach` (next 7 days, today highlighted, turning-age). **Birthday celebration modal** on athlete login — both athlete app AND `/member/book` (most athletes only book), greets the household so kids are greeted via the parent's login, branded (Forge logo + "Chris & Mimi von The Forge"), CSS confetti, once-per-day shared key in [utils/birthday.ts](utils/birthday.ts). **Waitlist OG** — coach can mark a waitlisted athlete Open Gym (off-cap → confirmed, no displacement); also added a **Remove** button to waitlist rows. ~11 commits. **Pending:** prod spot-checks; S380/S379 carry-overs.) -->

<!-- Older S380: Leaderboard grouping fix + Wellpass data-only sync + subscription/banner hardening + Stripe diagnosis. **Leaderboard** within-week dedup groups by `workout_name` ONLY (was `session_type|workout_name`). **Wellpass** release-day cap post-insert reconciliation (race fix); sync DATA-ONLY (never writes `wellpass_booking_restricted`, only suggests). **Subscriptions** athletes no longer notified "expiring"; Due banner confirm dialogs on Renew + dismiss, per-payer lead times. Anna Baur 1-year sub traced to accidental "Renew 1 Year" click, reverted. ~9 commits. -->


<!-- Older S378: Toolkit Exercise-tab usage fix + calendar deep-link + Wellpass badge rename. **(1)** [utils/movement-analytics.ts](utils/movement-analytics.ts) `getExerciseFrequency` keyed every result by a 2-week bucket in a Map → repeats of the same workout collapsed to one entry, all but one date dropped. Rewrote to record EVERY session + a `uniqueCount` of distinct workout names. Chip now `Used 5x (1 unique)`. **(2)** Calendar deep-link from usage modal → `/coach?date=YYYY-MM-DD`, read in a `useEffect` (not the useState initializer). **(3)** Wellpass status badge `&lt; min` → `blocked` (`55acdce`). -->


<!-- Older S377: Wellpass blocking system rewritten end-to-end. **Old rule** ("block if latest week below `min_checkins_required`") punished consistent users for one bad week. **New rule (3 gates, all must pass to be unblocked):** (A) 4-week login sum ≥ 3 × min (recent dormancy); (B) 12-week login sum ≥ 9 × min (annual pace); (C) shared identities only — 13-week login:attendance ratio ≥ 1.5× (under-padding the spouse-share deal). Each gate has a grace period (insufficient data = auto-pass). Math centralized in new [lib/coach/wellpassScoring.ts](lib/coach/wellpassScoring.ts), called by both `/api/coach/wellpass` (GET, for the tab) and `/api/coach/wellpass/import` (recompute). **WellpassTab UI:** old lifetime `+N/-N` Score column replaced by two new columns — **YTD %** + **All-time %** (logins ÷ target × 100; green ≥100 / amber 80–99 / red <80). Shared identities also show an inline **login:attendance ratio chip** under the name (green ≥1.5×, amber below). Status badge on blocked rows now carries a tooltip naming the rule that tripped. **Unblock all (N)** button added in the header — clears `wellpass_booking_restricted` on every blocked member via new [/api/coach/wellpass/unblock-all](app/api/coach/wellpass/unblock-all/route.ts). Also: DEV ENVIRONMENT section added at top (Synology Drive sync between Macbook + Windows PC; GitHub is source of truth); free-ports doc + session-open prompt patched for cross-platform paths. Pre-existing landmine: badge LABEL still says "&lt; min" but now fires for any of the 3 rules; rename to "blocked" pending Chris's call. See project-history for detail. [DONE S378: renamed to "blocked".]) -->

<!-- Older S376: Closed S375 carry-over list. Orphan athlete_profiles deleted (Alex Terbrack + Carla Rydval `carla-muecke@web.de` — NOT the real `c.rydval@web.de`). Athletes list tightened to ACTIVE-only ([app/coach/athletes/page.tsx](app/coach/athletes/page.tsx)). Karen 26/01 8-score re-entry + scaling on the other 2 Karen wods confirmed already done by Chris. Lifts-tab acronym fix for "Strict Overhead Shoulder Press" — set `barbell_lifts.acronym='OHP'`. Probe scripts deleted. Commits `52d689f` + `74fb764`. -->


<!-- Older S373: Bug-fix grab-bag + Wellpass Excel↔app verification. **(1) close-ten-card sharer guard:** Lenny Kleinert is a sharer on his mum Katja Brückner's 10-card; Chris closed/renewed on Lenny's row → created a duplicate card the holder-rollup ignored + tripped the `ten_card_sessions_used ≤ 10` constraint on Recalc ("Failed to backfill flags"). Diagnosed via one-shot scripts, cleaned the data (trimmed Katja's archived snapshot, deleted the bogus Lenny archive, reset Lenny to pure sharer), then added a guard at [app/api/coach/close-ten-card/route.ts](app/api/coach/close-ten-card/route.ts) that refuses the close when `ten_card_holder_id` is set and names the holder. Commit `0a922f0`. **(2) Copied-section duplicate-on-reopen:** dragging a section from +Workouts into an open workout saved fine but a duplicate appeared at the bottom on re-open. `handlePanelDrop` in [hooks/coach/useWorkoutModal.ts](hooks/coach/useWorkoutModal.ts) consumed `window.__draggedSection`/`__draggedWOD` but never cleared them; the modal-open effect re-injected the stale section next open. Cleared both globals after panel drop. Commit `fc8b937`. **(3) Reveal window 1h→2h:** booked athletes now see workout details 2 hours before class start (was 1h), changed in BOTH [components/athlete/AthletePageWorkoutsTab.tsx](components/athlete/AthletePageWorkoutsTab.tsx) and [hooks/athlete/useLogbookData.ts](hooks/athlete/useLogbookData.ts). **(4) Registration honeypot:** hidden off-screen `website` field on [app/auth/register-member/page.tsx](app/auth/register-member/page.tsx); [app/api/members/register/route.ts](app/api/members/register/route.ts) returns a success-shaped response but creates nothing when it's filled. Triggered by a bot signup (random name + dotted-Gmail + US phone); the pending-approval gate already blocked harm, this cuts the noise. **(5) Banner per-row dismiss:** [components/coach/SubscriptionsDueBanner.tsx](components/coach/SubscriptionsDueBanner.tsx) `handleDismiss` filtered by `memberId` only, so dismissing Claudia's erroneous cancelled-sub row also wiped her real upcoming-expiry row from the live view. Now filters by `memberId + kind`. (Persisted dismiss flag is still member-level but only affects lapsed rows — fine.) **(6) Wellpass tab expanded view:** shows each linked member's all-time gym attendance (same `get_all_members_attendance` RPC + 36500-day window as the Admin Attended chip) next to their name + the household lifetime Wellpass-logins total (sum of `weekly_history`). Also **paginated the wellpass check-ins query** in [app/api/coach/wellpass/route.ts](app/api/coach/wellpass/route.ts) — was past the 1000-row PostgREST cap (~1019 rows), silently dropping oldest weeks + undercounting the Score. **(7) Excel↔app verification:** `scripts/compare-wellpass-excel-vs-db.ts` parsed the live workbook with the real parser + diffed every person×week vs DB — **882 cells, 0 mismatches, 0 unmatched names.** Confirmed the sync workflow keeps app == Excel; re-import overwrites partial weeks so nothing is permanently lost. 3 commits + close.) -->

<!-- Older S371: Athlete Benchmarks leaderboard rework + 8 backlog verifications cleared + Karen 26/01 missing-scores root-caused. **Leaderboard rework** ([components/athlete/LeaderboardView.tsx](components/athlete/LeaderboardView.tsx) + [utils/leaderboard-utils.ts](utils/leaderboard-utils.ts)): Benchmarks subview now has parallel **Best** + **Last** columns, each showing result + DD/MM/YY date + scaling badges (Rx/Sc1/2/3) + Track (T1/T2/T3) — same layout in both cells. Header buttons toggle ranking by Best vs Last (default = Last); active button styled teal/bold/underlined. Date format flipped to `21/01/26` per Chris. Standalone Scale + Date columns removed; Athlete column gets `w-full` so Best/Last hug each other with a `pl-2` gap. `LeaderboardEntry` interface gained `lastResult`/`lastResultDate`/`lastScalingLevel{,2,3}`/`lastTrack`. `rankBenchmarkResults` takes optional `rankBy: 'best' | 'last'` (default `'last'`); for tied-on-primary, tiebreaker date direction flips with rankBy (ASC for best = earliest PR wins; DESC for last = most-recent attempt wins). Exercise list removed from the description box on both subviews per Chris's call ("athletes see the description, that's enough"); `exercises[]` still fetched + flowed for Planner/search use. **Backlog cleared:** S370 banner dismiss + Wellpass pause / S369 bookings column + Score / S368 athlete_subscription_end backfill / S367 Mac instability resolution / S366 load-3 + touch drag / S355 05/05 re-entry + women's lift records (Anneke verified via magic link) / S354 all 3 surfaces (Daniel Steller magic-link confirmed parent-self-block on kids classes) / S346 gym memberships flow — all marked ✅ in activeContext. Two stale ⏳ entries flipped: S367 webhook (S368 fixed) + Mac instability (S370 closed). **Karen 26/01/26 17:15 missing scores diagnosis:** 8 of 9 booked athletes' Karen results "disappeared" from the score-entry modal after Chris added a scaling option. Diagnostic scripts (`diagnose-karen-26jan-scaling-loss.ts`, `diagnose-karen-26jan-full.ts`) showed today's save was INNOCENT — wod.updated_at=today but the only surviving WSR (Madeleine) has updated_at=2026-05-09, untouched. The 8 "missing" scores DO exist in `benchmark_results` table (athlete-side leaderboard pools both tables, hence Chris sees them on the app), but the score-entry modal only queries `wod_section_results`. Root cause: 2026-05-09 S356 recovery batch — [app/api/score-entry/save/route.ts](app/api/score-entry/save/route.ts) writes WSR + benchmark_results sequentially with errors logged but loop continues; for those 8 athletes the WSR insert failed silently (likely S356 section-ID race), benchmark_results succeeded. S357 fix prevents future recurrence; adding scaling to the 2 other Karen wods (675cf187, 4479f1c3) is safe (`false→true` toggle doesn't null anything, S357 rename-detection protects existing WSRs). Chris will re-enter the 8 lost scores manually. 1 work commit + close.) -->

<!-- Older session detail lives in project-history/ + Current Status (Last 5 Sessions) below — trimmed S367-close. **Touch drag shipped end-to-end:** Mimi couldn't drag workouts on iPhone, Chris had to be finger-precise on Android. Three problems stacked: drag handle was `opacity-0 group-hover:opacity-100` so invisible on touch (no hover state); native HTML5 drag-and-drop is unreliable on iOS Safari; both native and polyfill clone the *draggable element* as drag image — here the 14px grip handle — producing an invisible-small ghost on Android too. Fixes: installed `drag-drop-touch` polyfill (auto-attaches on touch, configures press-and-hold mode with 500ms threshold so finger drift doesn't accidentally start a drag during page scroll); added `pointer-coarse:opacity-100` to make handle visible on any touch device; added a "lifted" CSS state on the source card during active drag (`opacity-50 ring-2 ring-teal-500 shadow-lg scale-[0.97]`) so the press-hold confirms visually; called `setDragImage` with the parent `.workout-card` so the ghost is a card-sized faded clone. Also wired `handleDragEnd` in [hooks/coach/useDragDrop.ts](hooks/coach/useDragDrop.ts) — `dragend` was never wired anywhere, harmless before but would have left cards permanently faded after the lifted state landed. Cross-platform: polyfill activates on Android too (it preventDefaults touchstart, suppressing Chrome's native drag), so Android now runs identical 500ms press-hold + ghost behaviour. Commits `8533c9c`, `980d232`, `c24425c`. **Leaderboard load-3 display fixed:** [utils/leaderboard-utils.ts:697](utils/leaderboard-utils.ts#L697) `formatResult` was hard-coded to handle `weightResult` + `weightResult2` only. Score-entry writes load 3 to `weight_result_3` correctly, the ranking math at lines 181/240 chains through all 3 slots, the per-slot scoring_fields gate already includes `load3` — only the display formatter was capped at 2. New local `joinLoads(...loads)` helper joins all non-zero load slots with `/` (e.g. `60/40/20 kg`); used in both the `case 'weight'` primary branch and the non-weight extras branch. Per-slot gating already happens upstream in `toLeaderboardEntry` so no re-gating needed. Surfaced via Sunday 24/05 10:00 Endurance #26.7 (modified) where Chris's 3-load entry showed only 2 loads on the athlete leaderboard.) **Household restriction:** [app/api/bookings/create/route.ts](app/api/bookings/create/route.ts) now resolves the booking member's household via `wellpass_identity_members` and counts confirmed bookings across all linked members. Pre-fix: per-member, so multi-person households got N× the limit (Sabrina + Wayne → 2/week; Peresyov 3-member → 3/week). Post-fix: 1/week regardless of who books. Service-role required for the household lookup (RLS hides cross-member rows from athlete's own auth context). German error updated to "Dein Wellpass-Haushalt hat in dieser Woche bereits einen Kurs gebucht…". Defensive fallback to per-member-only if no household link exists (shouldn't happen — recompute only flags linked members). **WellpassTab status label:** [components/coach/members/WellpassTab.tsx](components/coach/members/WellpassTab.tsx) hides `athlete_subscription_status` span unless value is `'active'`. The DB default for that column is literally `'expired'` so it was rendering on every Wellpass household even when the member had never had an in-app subscription — misleading and noisy on a tab focused on Wellpass household state. Teal `active` chip stays as a "paying twice" positive signal. **Albrecht household linked:** identity `71d046df-…` (Conny in Excel) → Cornelia + Luisa Albrecht (nickname mismatch — auto-linker can't match Conny↔Cornelia). **Peresyov household linked:** identity `aff1e2a7-…` (Dimitar in Excel) → Peresyov Dimitar + Peresyova Regina + Emilia Peresyov (S364 reverse-word-order would've caught Dimitar on next sync, but household members beyond the 1:1 name match always require manual linking). **Decisions parked:** Athlete Test 1 kept as permanent Wellpass test fixture (S364 leftover); Marion ↔ Michael Weber link confirmed already in place (earlier +1 manual adjustment guidance was stale, removed); nickname auto-linking rejected as too rare to warrant — Chris will rename in Supabase if needed. **Tiered booking release LIVE:** `booking_rules.wellpass_restricted_release_offset_minutes` column added (default 0 = backwards compatible); priority tier opens at `next_week_release_time`, Wellpass-restricted tier opens base+offset minutes later. Today's config: 16:00 priority, 18:00 restricted. `getMaxVisibleSessionDate` and `/api/bookings/create` both pass the viewer's `wellpass_booking_restricted` flag so server enforcement matches page-side. New `getNextReleaseInstant` drives a teal countdown banner on `/member/book` showing "Buchungen für nächste Woche öffnen in X Std. Y Min." — gated to release day (default Sunday) from 12:00 Berlin onwards until release fires, so it doesn't spam athletes Mon-Sat. Admin Booking Rules tab now has a "Wellpass restricted-tier offset (minutes)" input so the offset can be tuned without SQL. **Date-gate TZ bug fixed:** `getMaxVisibleSessionDate` returned end-of-Sunday 23:59:59 UTC which formats as Monday in Berlin browser, letting Monday next-week sessions render in the athlete booking list pre-release (API correctly rejected the booking attempt, visual-only leak). Shifted to 12:00 UTC — well inside Berlin Sunday. **Wellpass missing-from-Excel auto-block:** import now per-week zero-fills tracked identities missing from each week's sheet (Franziska Kary case — present in W20 but not W21, status stayed "ok" from stale W19 data); recompute runs on union of imported + all tracked IDs. **Wellpass tab Block button always visible** on tracked rows (was gated on `status === 'below_threshold'`). **Wellpass auto-linker reverse-word-order fallback** — handles "Keip Andreas" Excel vs "Andreas Keip" member.name (Andreas Keip case, second time after Martina Fenster S361). **Athlete page capacity now matches coach:** SELECTs is_og + is_trial + trial_names, computes `confirmed.filter(!og && !trial).length + trial_names.length` (Sandra Lederle case: coach showed 6/10, athlete showed 7/10, off-by-one). Attendees endpoint excludes OG/trial from the public "Also attending" list but still treats viewer's own OG/trial booking as "I'm in this session". Manual test setups: Athlete Test 1 (full WP restricted flow tested + flag cleared mid-session), Franziska Kary (flag flipped to test before deploy), Andreas Keip (manually linked before auto-linker shipped).) -->

---

## 🖥️ DEV ENVIRONMENT (read first, every session)

**Chris works on TWO machines, with the project folder synced via Synology Drive:**

- **Macbook** = primary development machine. Code is written, committed, and **pushed to GitHub** there.
- **Windows PC** (this machine, path `c:\Users\Chris\SynologyDrive\AI Development\forge-functional-fitness`) = secondary, used for sessions like this one.

**Source of truth = GitHub `origin/main`**, NOT the local working tree on Windows.

Synology Drive syncs files in the background and is **not git-aware**. When Chris pushes from the Macbook and then opens a session on the Windows PC, Synology may not have finished syncing — so `git status` on Windows shows ghost "uncommitted changes" (modifications, deletions, untracked files) that are really just stale sync artifacts predating the Macbook push.

**Session-start workflow on Windows:**

1. `git fetch origin`
2. If `git status` shows local changes Chris didn't make in *this* session, they're sync artifacts — run `git reset --hard origin/main && git clean -fd` to discard them and match remote exactly. Confirm with Chris first only if there's genuine uncertainty.
3. Then proceed as normal.

**Do NOT** treat ghost local changes as a merge conflict to resolve — that wastes a session diagnosing nothing. Trust GitHub.

---

## ⚠️ CRITICAL RULES

| Rule | Detail |
|:---|:---|
| **Mandate** | Read `memory-bank/workflow-protocols.md` only when task actually needs it |
| **Database Safety** | Run `npm run backup` BEFORE any migration or risky change |
| **Agent Use** | Agent only for 3+ step tasks, multi-file changes, genuine unknowns |
| **Efficiency** | Target < 50% context. See `Chris Notes/AA frequently used files/Claude open or close session.md` for rules |
| **Context Monitoring** | 50/60%: alert. 70%: STOP, summary+commit+new session. 80%: critical |

---

## ⚡ Next Session Kickoff

_Updated at every session close. The "first 5 minutes of tomorrow" — read this immediately after the regular activeContext + latest project-history scan._

**🚨 Next session — S408 shipped the Movement Info bar (`a9399bb`, 1 commit, pushed, tsc+lint+build clean). Chris has NOT yet tested it live — that's first action #1. Nothing blocking. ⚠️ The still-pending prod verifications from S405/S402 were NOT revisited again this session — they're now three sessions stale; see the list below.**

**S408 functional check for Chris:** open tonight's (or any) session for edit → expand **Movement Info** (renamed from *Movement Demos*) → click the **name** on a chip (not the ▶) → the full library description should appear with its line breaks intact, with a "Play demo video" button underneath if that movement has one. Confirm the **▶ still plays the video in one click** — that path must not have regressed.

**S408 pitfalls to watch (full list in the project-history file):**
- **False-positive chips.** The match pool went 298 → 713, so a coaching cue containing a movement name can now produce a chip Chris didn't intend. Fix = tighten the matcher (raise the ≥4-char floor, or exact-match only for description-only entries), **never** strip descriptions from the library.
- **The count badge will jump** — it counts everything with reference material now, not just video. Expected; if it reads as noise, count video only and surface descriptions on demand.
- **Coach-side only.** No new athlete-facing surface was added. Don't assume athletes can see library descriptions.
- **`MatchedExerciseVideo` no longer exists** (`MatchedExerciseInfo`) — stashed work referencing it won't compile.
- **`section-video-matcher.ts` is now a misnomer** (it matches descriptions too); left as-is to avoid churn, header comment says so.

**S408 tweak levers (only if Chris asks):** (a) revert the *Movement Info* label to *Movement Demos*; (b) raise Intent/Stimulus `maxLength` above 500 — but clamp the TV render at the same time (`app/tv/[id]/page.tsx` prints intent untruncated at up to `text-5xl` above the workout); (c) the TV shows intent **regardless of the "Show to athletes" checkbox** — a real gap if athletes can see the screen.

**First actions / open items:**
1. **⭐ Test the S408 Movement Info bar live** (checklist above). Chris said he'd check it thoroughly.
2. **✅ DONE — `exercises` pagination sweep (S409).** All 11 unfiltered reads of the table now go through `fetchAllExercises()` in [utils/fetch-all-exercises.ts](utils/fetch-all-exercises.ts), which pages past the 1000-row cap. Verified end-to-end: returns exactly 716 rows, no dupes/gaps, at page sizes down to 7 (103 round-trips). Sibling library tables counted and safe (`barbell_lifts` 20, `benchmark_workouts` 26, `forge_benchmarks` 17). **Nothing left to do here.**
3. **⭐ VERIFY THE ATTENDEE FIX (S405) — still not done after 3 sessions.** Michael Städele reported "5/10 booked but no names underneath", persisting even after others booked. Fixed two bugs: crash on sole-attendee sessions ([attendees/route.ts:68](app/api/bookings/attendees/route.ts#L68) missing `?? []`) + stale phone HTTP cache (`cache:'no-store'` added client + server). Have Michael reopen a WOD where names were missing → they should now show. **If it STILL fails:** the untouched 3rd suspect is `getSession()` returning null mid-refresh ([book/page.tsx:348](app/member/book/page.tsx#L348)) silently skipping the fetch — ask him whether a **full reload** fixes it (→ token/auth path) or not (→ elsewhere) before touching auth code.
4. **Verify the mobile Planner fixes (S405)** on Chris's phone: (a) pattern rows show just the count, more of the name visible; (b) Adults Session chips (All/WOD/Foundations) wrap under the Track toggle, with "Session:" label beside its chips. If pattern names STILL clip to 1-2 letters, the name column width itself is the constraint (it has `truncate`) — next lever is widening the sticky name column on mobile.
5. **Test the mobile WOD editor on BOTH phones (S402, `cb573c0`+`8b3d9b0`).** (a) **full-screen** (no Workouts/Schedule nav peeking above); (b) tap a section's text field → **Library / + Section bar stays pinned**; (c) **Mimi's iPhone** — swiping the modal must NOT pan sideways.
6. **⚠️ WOD-overwrite data-loss (S404, no fix shipped).** Copying a workout over a **published single-session** WOD hard-deletes the original ([useWODOperations.ts:781-812](hooks/coach/useWODOperations.ts#L781-L812) orphan cleanup); if it was created the same day it's not in the nightly backup either → unrecoverable without Supabase PITR. **Possible guard to offer Chris:** confirm-before-overwrite (or soft-delete/undo) when a copy would replace a *published* WOD with no other session references. Only build if Chris wants it.
7. **S407 — coach-only park/block reason read check.** Park an athlete with a reason → confirm it shows on the Parked tab + "+ add reason"/edit works. Blank reason when one was typed = a visibility bug in the `coach_member_notes` coach-read policy, **never** a leak (anon/non-coach read was verified blocked).
8. **S404 colour note** (only if Chris asks "why didn't my groups change colour"): changing `PATTERN_COLORS` doesn't recolour existing groups (stored hex) — click a dot → pick from the popover. A few picks are pale (`#C2DFFF`, `#F4CDE0`, `#E8F86D`) — offer to deepen.


## 🎯 Project Overview

CrossFit gym management app — WOD creation, analysis, member booking, athlete performance tracking.

**Stack:** Next.js 15 App Router · TypeScript · Tailwind · Supabase (PostgreSQL + RLS) · Supabase Auth · Recharts · Stripe (live mode)

**Deployed:** `https://app.the-forge-functional-fitness.de` (Stripe identity verified Session 193)

**Units:** Metric (kg, cm, m)

---

## 🗄️ Data Models (Core Schema)

```
Users (Supabase Auth)
├─ auth.users (id, email)

Coach Tables
├─ wods (id, date, session_type: TEXT, workout_name: TEXT, workout_week: TEXT, sections: JSONB [content, lifts[], benchmarks[], forge_benchmarks[], scoring_fields], is_published, publish_time, publish_sections, publish_duration, google_event_id, coach_notes: TEXT, title: TEXT [DEPRECATED - use session_type])
├─ section_types (id, name, display_order)
├─ workout_types (id, name)
├─ workout_titles (id, title)
├─ exercises (id, name [UNIQUE], display_name, category, subcategory, equipment[], body_parts[], difficulty, is_warmup, is_stretch, search_terms, search_vector [GIN indexed])
├─ user_exercise_favorites (id, user_id, exercise_id, created_at [UNIQUE user_id + exercise_id])
├─ naming_conventions (id, category [equipment|movementTypes|anatomicalTerms|movementPatterns], abbr, full_name, notes)
├─ resources (id, name, description, url, category)
├─ tracks (id, name, description, color)
├─ weekly_sessions (id, date, time, workout_id, workout_type: TEXT, capacity, status, is_locked: BOOLEAN [NULL=auto, true=locked, false=unlocked])
├─ benchmark_workouts (id, name, type, description, display_order, has_scaling)
├─ forge_benchmarks (id, name, type, description, display_order, has_scaling)
├─ barbell_lifts (id, name, category, display_order, equipment)
├─ programming_notes (id, user_id, title, content [markdown], folder_id, created_at, updated_at)
├─ note_folders (id, user_id, name, display_order, created_at, updated_at)
├─ coach_tracked_exercises (id, user_id, exercise_id, display_name, active, created_at)
├─ movement_patterns (id, user_id, name, description, track [adults|kids], created_at, updated_at)
├─ movement_pattern_exercises (id, pattern_id, exercise_id)
├─ programming_plan_items (id, user_id, pattern_id, planned_date, created_at)

Member Tables
├─ members (id, email, name, status, membership_types[], account_type: primary|family_member, primary_member_id, display_name, date_of_birth, relationship, class_types[] [ekt|t|cfk|cft], gender [M|F|null], whiteboard_name, subscription_tier, athlete_subscription_start, athlete_subscription_end, athlete_subscription_status)
├─ bookings (id, session_id, member_id, status: confirmed|waitlist|cancelled|no_show|late_cancel|coach_cancelled)

Athlete Tables (linked to members.id)
├─ athlete_profiles (id, user_id, full_name, emergency_contact)
├─ workout_logs (id, user_id, wod_id, result, notes)
├─ benchmark_results (id, user_id, benchmark_id, forge_benchmark_id [XOR], benchmark_name, benchmark_type, result_value, scaling_level, scaling_level_2, scaling_level_3, result_date)
├─ lift_records (id, user_id, lift_name, weight_kg, reps, rep_max_type ['1RM'|'3RM'|'5RM'|'10RM'], rep_scheme, calculated_1rm, notes, lift_date, wod_id [CASCADE])
├─ wod_section_results (id, user_id, member_id, wod_id, section_id, workout_date, time_result, reps_result, weight_result, weight_result_2, weight_result_3, scaling_level, scaling_level_2, scaling_level_3, rounds_result, calories_result, metres_result, task_completed, track [SMALLINT 1/2/3 or NULL], whiteboard_name)

Achievement Tables
├─ achievement_definitions (id, name, description, category, branch, tier, difficulty, created_at)
├─ athlete_achievements (id, user_id, achievement_id, achieved_date, notes, awarded_by, created_at [UNIQUE user_id + achievement_id])

Social Tables
├─ reactions (id, user_id, target_type ['wod_section_result'|'benchmark_result'|'lift_record'], target_id, reaction_type ['fist_bump'], created_at [UNIQUE user_id + target_type + target_id])

Athlete Tools
├─ timer_presets (id, user_id → auth.users, name, intervals JSONB [{work, rest}[]], created_at, updated_at [UNIQUE user_id + name])
```

**Workout naming:** `session_type` (WOD, Foundations, Kids & Teens…) + optional `workout_name` + auto-calculated `workout_week` (ISO, UTC-based). Unique identifier = `workout_name + workout_week` (falls back to date if null).

---

## 📍 Current Status (Last 5 Sessions)

**Session 408 (2026-08-26 — Opus 5) — MOVEMENT INFO BAR: EXERCISE DESCRIPTIONS IN THE WORKOUT BUILDER (1 COMMIT + CLOSE, pushed, tsc+lint+build clean, Chris testing live):**
- **The ask.** Chris was copying movement mechanics (e.g. the 3 progressive levels of the Ring Muscle-Up Floor Assisted drill) into a section's Intent/Stimulus or Notes — duplication that burns session-level fields on movement-level content. He wanted library descriptions reachable from the modal the way attached videos already are.
- **Why it was nearly free (`a9399bb`).** The top bar already scanned section text and matched names via `matchAllSectionsExercises`. Descriptions were absent for two reasons only: the fetch didn't select `description`, and the matcher hard-dropped `if (!ex.video_url) continue`. Coverage counted live: **716 exercises, 713 with a description, 298 with a video** — the bar fired on ~40% of movements, now ~100%.
- **Shipped.** [section-video-matcher.ts](utils/section-video-matcher.ts) keeps video **OR** description and carries the text (`MatchedExerciseVideo`→`MatchedExerciseInfo`; matching logic incl. the S384 longest-match overlap pass **untouched**). New [MovementInfoModal.tsx](components/coach/MovementInfoModal.tsx) — `whitespace-pre-wrap` so drill levels keep line breaks, `z-[105]` **below** `ExerciseVideoModal`'s `z-[110]` so Play opens on top, portals to `<body>`. [MovementDemosBar.tsx](components/coach/MovementDemosBar.tsx) chips = two buttons in one shell (**▶ video stays ONE click**, name opens description, blue ℹ marks it); label *Movement Demos*→*Movement Info*. [useWorkoutModal.ts](hooks/coach/useWorkoutModal.ts) selects `description` + paginates.
- **Rejected: a separate description bar.** 713 vs 298 means the video set is almost entirely a *subset*, so the same movement would list in both rows; two collapsible headers also eat space above the sections in a modal with two sessions of mobile-space work (S402, S405). Would only have been right if video/text served different purposes (play for the class vs read before coaching) — Chris confirmed they don't.
- **Intent/Stimulus left alone** at `maxLength={500}` (auto-growing textarea, [WODSectionComponent.tsx:614](components/coach/WODSectionComponent.tsx#L614)). If it resurfaces: raise the cap **and** clamp the TV render — `app/tv/[id]/page.tsx` prints intent untruncated at up to `text-5xl` above the workout, and shows it regardless of the "Show to athletes" checkbox.
- **Verified** against the live catalogue: `Ring Muscle-Up Floor Assisted drill` (video Y, 529ch), `Back Squat` (**no video**, 231ch — previously dropped entirely), `KB Dead Bug` (video Y, 319ch).

**Session 407 (2026-08-25 — Opus 4.8) — MEMBERS/PLANNER/SUBSCRIPTIONS BATCH (8 COMMITS + 2 MIGRATIONS + CLOSE, all pushed, tsc+build clean, tested live by Chris):**
- **Subscriptions Due banner overhaul (`5dcd440`).** Lapsed warnings now **persist until the coach renews or dismisses them** (removed the 14-day floor on both cash + Stripe lapsed queries). **Cash grace 4→7 days** before the coach auto-expire cuts access ([useMemberData.ts](hooks/coach/useMemberData.ts)), and the athlete [PaymentDueBanner.tsx](components/athlete/PaymentDueBanner.tsx) `GRACE_DAYS` 4→7 so the in-app "pay" reminder spans the whole week. Dismissed 2 ex-beta-testers (Torben Stoffer, David Montgomery) via `lapsed_banner_dismissed_at`.
- **Planner usage-recency dots in the Movement Library (`b266eb4` + `3bf5716`).** The workout create/edit Movement Library popup shows a recency dot (same bands as Planner chips) on all 4 tabs (Exercises incl. Favorites/Recent, Lifts, Benchmarks, Forge) + "last programmed" tooltip + shared legend. New [utils/exercise-recency.ts](utils/exercise-recency.ts) = single source of truth for the bands + `makeRecencyMapLoader` (one session-cached full-history map per library type). Planner `PatternExerciseChips` de-duped onto it.
- **Wellpass — Lani Neumann linked to mum Katja's household (`3c9ffd9`, DB-only).** No UI for a 2nd household member (Excel sync auto-links only one per Wellpass name) → inserted the `wellpass_identity_members` row directly.
- **Park/Block reason feature (`2c7b29a` + `95bc2e0` + `2734a6a`; migrations `20260824000000` then `20260825000000`).** Capture WHY on park/block via an optional field on the shared confirm dialog (`confirmWithReason`, keeps Block's red styling); inline "+ add reason"/edit on Parked & Blocked rows for the 23 pre-existing parked members (`ReasonEditor` + status-reason route). **Then hardened:** reasons were columns on `members`, athlete-readable for their own row (RLS `auth.uid()=id`) → a parked athlete could fetch their own. Moved to coach-only **`coach_member_notes`** (RLS `role=coach` only; no athlete policy), writes via service role (best-effort), coach list-read merges for Parked/Blocked tabs, `members` columns dropped. Verified with anon key: non-coach read returns 0 rows though the row exists.
- **Parked-athlete rebook notification (`aec03be`).** Parked athletes keep booking access → when one self-books, coaches get a push + notification-log alert ("Parked athlete is back — Restart them?") linking to Members; service-role parked read in [bookings/create](app/api/bookings/create/route.ts), fire-and-forget. Fires per booking until Restart.

**Session 405 (2026-08-21 — Opus 4.8) — MOBILE PLANNER UX + INTERMITTENT ATTENDEE-LIST BUG (2 real bugs) (5 COMMITS + CLOSE, all pushed, tsc+build clean, awaiting Chris's live test):**
- **Planner — hide "exercises" word on mobile (`299b010`).** [PatternManager.tsx:318](components/coach/analysis/PatternManager.tsx#L318) — mobile shows just the count number, `sm+` shows `N exercises`, so more of the pattern name fits (names were truncating to 1-2 letters). Name column still has `truncate` → column width is the remaining limit if they still clip.
- **Planner — Session sub-filter wrap on mobile (`f34ae2d` + `df5cf6d`).** Adults All/WOD/Foundations chips ([PlannerSection.tsx:670](components/coach/analysis/PlannerSection.tsx#L670)) ran off the right edge — inner Track+Session flex row had no `flex-wrap`. Added it, then grouped "Session:" label + chips into one flex unit so they wrap together (first pass stranded the label alone above the chips).
- **🐛 Intermittent attendee list — TWO bugs (`28ab186` + `db2ad5e`).** Michael Städele: "5/10 booked, no names underneath", persisting even after others book. Count and names come from DIFFERENT sources (count baked into session list; names from a separate `/api/bookings/attendees` GET). **Bug A (crash):** [attendees/route.ts:68](app/api/bookings/attendees/route.ts#L68) iterated `sessionBookings[sid]` without the `?? []` guard used at line 93 → a session where the viewer is the sole confirmed non-OG/non-trial booking → `for...of undefined` throws → **500 wipes names for the whole batched request**. Hits early bookers; self-heals as others book → intermittent. **Bug B (stale cache):** attendee GET had no cache directive + a URL stable per booked-session set → mobile browsers cached an early empty response and served it even after members booked in. Added `cache:'no-store'` (client) + `Cache-Control:no-store` (server). Bug B matches "still empty after they booked". Weak 3rd suspect left untouched: `getSession()` null mid-refresh silently skips the fetch — didn't touch working auth on a hunch.

**Session 404 (2026-08-15 — Opus 4.8) — PLANNER PATTERN-COLOUR OVERHAUL + WOD-OVERWRITE RECOVERY INVESTIGATION (6 COMMITS + CLOSE, all pushed, tsc+build clean, all tested live):**
- **Bigger group dots (`c236a33`).** Movement Patterns colour swatch `w-3`→`w-5` (12→20px); stays within the row's existing height so rows don't grow.
- **Click-to-pick colour popover (`ed34e6f`).** New shared `ColorDot` component: clicking a pattern's dot (and the new-pattern swatch) opens a small palette popover instead of cycling one colour per click; closes on selection / outside-click.
- **Palette iterations (`8c82716` → `c7ca2f9` → `2b6c215` → `cfa4c34`).** Fixed the rose-pink that read like red → added colours → rebuilt as a spectrum-ordered max-diversity 12 → **applied Chris's own 12-colour pick** (maroon→pink: `#770909,#F20202,#F99639,#E8F86D,#80F982,#8AFFF3,#C2DFFF,#5196D6,#1A0BBC,#830BF4,#E367F4,#F4CDE0`). Built an interactive **colour-picker Artifact** (native pickers, real 20px dot previews, closest-pair ΔE flag, selectable hex box — clipboard API is blocked in the artifact sandbox). Caveat: changing `PATTERN_COLORS` doesn't recolour existing groups (stored hex) — re-pick via the popover; a few picks are pale.
- **⚠️ WOD-overwrite data-loss (no code).** Chris copied a workout over a published **single-session** WOD created the same day. `handleCopyWOD` inserts a new wod + repoints the session, then hard-deletes the old wod once it's orphaned (+ cleanup-results). Created-today → not in the nightly backup. Verified via service-role script that only the pasted copy survived. **Unrecoverable** without Supabase PITR; Chris recreated it manually. Possible future guard: confirm/undo on copy-over of a published WOD.

**Session 403 (2026-08-14 — Opus 4.8) — PLANNER POWER-TOOLS BATCH + LIFT PRESETS + SECTION MONOSPACE TABLES (13 COMMITS + CLOSE, all pushed, tsc+build clean, all tested live):**
- **Planner last-5-unique-workouts popovers (`7a5c8de`; name-fix `402be07`).** Click an exercise chip (past-dot detail panel, expanded-group chips, or Movement Patterns chips) → its last 5 unique workouts (deduped by `workout_name`). Fixed by matching on NAME not id — duplicate "Good Morning" records (`barbell-good-morning` + `good-morning`) meant the pattern's linked id missed the frequency map; `getExerciseFrequency` now emits `display_name`, map keyed by name+display_name.
- **Chip staleness from full history + new colour bands (`56d91ff`).** Was coloured off `computePatternGaps` (~16wk window) → false "Never" for older exercises; now uses the eagerly-loaded full-history map. ≤14 green / 15–28 yellow / 29–60 orange / 61–90 red / 90+ light-grey / Never = faded dark-grey (retire cue).
- **Movement Patterns move tools (`7f6c097`, `3352103`, `a85bfc2`, `c329ef1`).** Drag a chip's handle onto another group's row to move; **Select** mode → tick several → **Move|Copy** toggle → target group. Per-group **sort** toggle (recent ↔ stale/never-first). Junction-table only, no scaling risk.
- **Collapsible Planning Grid (`856f6ed`)** (chevron hides grid + date/RM controls; persisted). **Adults session sub-filter All/WOD/Foundations (`6fe0fa6`)** (hidden on Kids).
- **Per-lift rep-scheme presets (`439650e`).** Configure Sets/Reps modal → save/name a lift's scheme (constant/variable/RM), re-apply per-lift in one click. New table `lift_rep_scheme_presets` (RLS per user; upsert on user_id,lift_id,name) — Chris ran the SQL, confirmed.
- **Per-section "Keep table layout (monospace)" toggle (`f50aa01`, preview `401f1db`).** Opt-in `section.monospace` renders `content` font-mono on ALL display surfaces (calendar/TV/athlete/logbook/leaderboard/publish) so pasted strength-scheme tables keep columns; off by default (old workouts untouched). Editor is already mono → added a live display-font preview under the checkbox. For Chris's Claude-Desktop tables shown on the gym screen. Docs: Planner "How it works" + Feature Overview updated.

**Older sessions (57-401):** See `project-history/` folder.

---

## 🚨 Known Open Issues

- **Mac Chrome hang (recurring, system-level)** — Chris's Macbook: after working a while, apps bounce in dock but won't launch ("Google Chrome is not responding"). Only full Mac restart fixes it. Happens increasingly often. Directly affects Forge pushes: Chrome in half-dead state = stuck GCM "Connecting", so Mac push never arrives. Not a Forge code issue; dedicated session needed. Diagnostic starting points: Activity Monitor Memory Pressure, disk free %, Chrome Helper memory leaks, `~/Library/Logs/DiagnosticReports/` for spindumps. (Session 292.)
- **Mac push delivery (downstream of above)** — Mac never receives FCM pushes even with clean DB subs + healthy SW. `chrome://gcm-internals/` shows Connection State "Connecting". Will auto-resolve once the Chrome-hang root cause is fixed. Android push unaffected.
- **Test endpoint doesn't cleanup 410s** — `app/api/notifications/test/route.ts` bypasses `sendToSubscription` helper so expired subs aren't auto-deleted when you click Send Test. Low priority — production flows still clean up 410s. (Session 292.)
- **iPhone search bug (latent)** — same `readOnly` anti-autofill hack exists in `components/coach/SearchPanel.tsx:946` (Analysis page search). Deferred Session 282.
- **Workout search 2000-row limit (S349)** — `useCoachData.ts:searchWODs` caps results at 2000 (bumped from 500 in S349). A `[search-limit-tripwire]` console.warn fires at 90% of the limit so we'll see it before older WODs start disappearing from unfiltered searches. When the tripwire fires: see `memory-bank/database-and-growth.md` for the four UX options (A/B/C/D) and pick one.

**Pre-deployment:** All CRITICAL/HIGH/MEDIUM items done. LOW items (28 files >500 lines) deferred per Session 260.

**Exercise naming conventions (Session 149):**
- "Lunge Walking" (not "Walking Lunge")
- "Jump Rope Double-Unders (DUs)"
- KB Swing default = American (AKBS)
- Generic "Row" in benchmarks = C2 Rower

---

## 📋 Next Immediate Steps

1. **S408 — test the Movement Info bar live.** Expand *Movement Info* in the create/edit modal → click a chip's **name** (not the ▶) → full description with line breaks + a Play button if it has a video. Confirm **▶ still plays in one click**. Watch for false-positive chips (match pool went 298→713).
1. **S407 — mostly tested live; one coach-only read check left.** Park an athlete with a reason → confirm it shows on the Parked tab and inline "+ add reason"/edit works (the RLS-protected coach read path — anon/non-coach block already verified). Optional tweaks only if asked: dedupe the parked-rebook notification to once-per-athlete; a "dismiss all older than N" on the now-unbounded lapsed list.
1. **S404 — nothing to verify** (colour/UX, tested live). If Chris asks, offer a **confirm/undo guard** for copy-over of a published single-session WOD (the unrecoverable data-loss path), and offer to deepen the pale palette picks (`#C2DFFF`,`#F4CDE0`,`#E8F86D`).
1. **S403 — nothing to verify.** All Planner/preset/monospace features were built + confirmed working live in-session; `lift_rep_scheme_presets` SQL run by Chris. Optional follow-on (only if asked): drag from Uncategorised into a group (already served by its "Move to…" button).
1. **S402 — Test mobile WOD editor on both phones** (`cb573c0`+`8b3d9b0`): full-screen (no nav peeking), Library bar stays pinned when keyboard opens, no horizontal pan on Mimi's iPhone. **Only S402 item still needing device confirmation.**
1. **S402 — Verify Whiteboard 32.1 "The Ghost" 17:15 modal** (`f33363c`): 2026-08-07 17:15, 10 rows incl Justine DNF; leaderboard ranks within Rx/Sc1/Sc2. Julia Weihe deliberately skipped.
1. **S401 — Spot-check whiteboard 31.1 + 31.3 on prod** (see ⚡ Next Session Kickoff): 31.1 27.7 17:15 modal + FS-kg/tier leaderboard; 31.3 29.7 18:30 OHS modified flag + new DUs (Scaling 2) column renders.
1. **S401 — Verify score-entry fix on prod** (`4618f93`): results modal lists booked members + trial/drop-in only, no stray whiteboard-name rows from intro text.
1. **S400 — Verify the 3 fixes on prod** (see ⚡ Next Session Kickoff): Restore-session button reopens + restores bookings; Jacht kids show shared `6/10 · used N` chip; auth self-heal (watch for freeze recurrence).
1. **S399 — Verify the Private event feature on prod** (see ⚡ Next Session Kickoff for the checklist): exercises excluded from search/Planner, name still findable, "Private events" filter lists them, athletes never see them. Test case = Sommersportfest 2026-07-22.
0. **S372 — Verify athlete Logbook multi-scale fix on prod.** After Vercel deploy: open athlete app → Logbook → date with a multi-scale benchmark entry (e.g. Friday 20.02.26 17:15 "Tabata This"). Expected: all 3 Scale levels + 3 loads show, matching the Leaderboard. Spot-check another benchmark/forge section. Single-scale entries must be unchanged.
0a. **S371 — Verify athlete Benchmarks leaderboard rework on prod.** Athlete app → Leaderboard → Benchmarks → any benchmark. Expected: Best + Last columns, clickable headers toggle rank, default = Last. Description-only above the table.
0b. **S371 — Re-enter Karen 26/01/26 17:15 scores manually.** 8 athletes' scores exist in `benchmark_results` but not `wod_section_results`; modal looks empty. Names + values in project-history S371. Coach entry takes priority on leaderboard so dedup is automatic.
0c. **S371 — Add scaling option to the 2 other Karen wods.** `675cf187` (18:30 26/01, 6 WSRs) + `4479f1c3` (28/01, 4 WSRs). Safe (`false→true` toggle doesn't null data).
0d. **Sunday Wellpass sync (gated, weekly).** `/coach/members` → Wellpass → Sync from Excel before sessions go live each Sunday. Skim `blocks_applied`/`blocks_cleared` list.
0e. **Paper-card sync remainders (~12 holders)** — parked pending parent contact. List via `npx tsx scripts/list-ten-card-no-purchase-date.ts`. Frida Engels has a duplicate row from a parent-kid mix-up — clean up when speaking to mum.
0f. **Verify RM-test distinction on deploy (S341).** Toggle `[ All | RM Testing only ]` on the planner.
1. **Verify the AKBS Deadlift leaderboard fix on the production deploy (S338).** Open the WOD's leaderboard for "WOD Pt.3" — Chris (47/20) should rank above Madeleine (48/12). Spot-check Back Squat Testing, BFS 5x5, and Strict Movements/KBOHC.
1b. **Finish retroactive 10-card bookings for the 8 remaining athletes (S336 carry).** Run `npx tsx scripts/probe-unbooked-whiteboard-athletes.ts` for the per-athlete date list. Manually book each via the Session Management modal — counter increments correctly when status lands as `confirmed`. After each athlete, glance at the chip — ⚠ glyph means a mismatch with `ten_card_sessions_used`; open TenCardModal → Recalc → Save to clear (or leave as a documented override per Rosita's case).
1c. **Re-evaluate booking-window countdown thresholds (S335)** — currently amber under 2h, red under 30m on Book a Class cards. If athletes report the warning fires too early/late after a few days of real use, tune the breakpoints in [app/member/book/page.tsx](app/member/book/page.tsx) `renderBookingCountdown`.
1d. **Re-evaluate Statistics chip dimming thresholds after 1–2 weeks of use** — currently 10%/30% of max count in [components/coach/analysis/StatisticsSection.tsx](components/coach/analysis/StatisticsSection.tsx). If categories with one dominant exercise (e.g. Back Squat in Olympic Lifting) end up with most chips dimmed and the very-low tier loses signal, bump to 20%/50% or switch to quantile-based ranking.
1. **Set up `next-intl` i18n (DE/EN bilingual)** — Chris plans to commercialize. The ~11 inlined German strings from S317 should migrate to `messages/de.json` + matching `messages/en.json`. ~1 day of dedicated work. Stop adding more inline German until this lands. Memory: `project_commercialization_and_i18n.md`.
2. **Verify SPF/DKIM/DMARC + test reset flow on deployed app (S297 follow-up)** — Resend → Domains → `the-forge-functional-fitness.de` should show all ✅. Then test the full reset flow end-to-end on live app.
3. **Mac Chrome hang investigation** — dedicated session. Start with Activity Monitor (Memory Pressure + Chrome Helper), disk free %, update status, then hang reports in `~/Library/Logs/DiagnosticReports/`. Will fix Mac push as a side effect.
4. **Whiteboard duplicate entries** (see `memory/project_whiteboard_duplicates.md`) — uncommitted changes from Session 251 need reviewing/committing. **Note:** S305 backfill may have largely resolved this by retroactively booking whiteboard names; re-evaluate before doing the S251 work.
5. **Score-entry API filter (deferred from S289)** — `app/api/score-entry/[sessionId]/route.ts` only filters bookings by `status='confirmed'` (and now `is_og=false`) and ignores `members.status`. If unapprove should cascade to hide bookings, filter in API or cascade-cancel bookings.
6. **Test endpoint 410 cleanup** (deferred from S292) — route `app/api/notifications/test/route.ts` through `sendToSubscription` so expired subs auto-delete on Send Test.
7. **Improve `fetchWODs` error logging** — when Supabase errors stringify as `{}` in the catch block (as happened in S322 with the missing `is_og` column), the cause is hidden. Same fix as S318 booking-error toast: extract `.message`/`.code`/`.details`/`.hint`. Low priority.
8. **Audit other diagnostic scripts in `scripts/` for anon-key blind spot** (S323) — `check-ghost-scaling.ts` and others use `NEXT_PUBLIC_SUPABASE_ANON_KEY`; if they query RLS-protected tables they may silently return empty. Switch to service role.
9. **Optional: derive a "Guardian" badge on MemberCard** (deferred from S324) — automatically show when a member has any rows pointing at them via `primary_member_id`. Distinct from the existing "Guardian Only" toggle (which means "doesn't train"). Was discussed in S324 but not built; the `guardian_only` binary toggle covers the immediate need.

---

## 💰 Business Model (Session 270)

- **Free:** All active members can book classes (no payment required)
- **10-Card:** €150 for 10 gym sessions (drop-in alternative, separate from app)
- **Athlete App — Forge:** €8/mo or €80/yr (logbook, records, leaderboards, achievements)
- **Athlete App — Wellpass:** €10/mo or €100/yr (same features, for Wellpass members)

---

## 🗂️ Resources

- **Detailed history:** `project-history/` folder
- **Gotchas & patterns:** `memory-bank/lessons-learned.md`
- **Workflow rules:** `memory-bank/workflow-protocols.md`
- **Tech details:** `memory-bank/techContext.md`
- **Code patterns:** `memory-bank/systemPatterns.md`
- **Deployment plan:** `Chris Notes/Deployment/deployment-plan.md`
- **Orphan diagnostics:** `Chris Notes/Database & Supabase/supabase-orphan-check-queries.md`
